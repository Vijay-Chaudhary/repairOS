[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
pythonpath = . apps
addopts =
    --cov=apps
    --cov-report=term-missing
    --cov-fail-under=80
    -v
filterwarnings =
    ignore::DeprecationWarning
