# Backend API base URL (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:8000

# WebSocket base URL
NEXT_PUBLIC_WS_URL=ws://localhost:8000

# App URL (used for PWA / OAuth redirects)
NEXT_PUBLIC_APP_URL=http://localhost:3000
