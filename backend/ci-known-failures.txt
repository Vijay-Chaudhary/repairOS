# ─────────────────────────────────────────────────────────────────────────────
# CI known-failing tests (deselected by the pipeline test gate).
#
# Currently EMPTY — all previously known failures have been fixed:
#   - reports/finance permission-slug drift (aligned view/test slugs to the seed)
#   - billing duplicate-invoice assertion (400 → 422 BusinessRuleViolation)
#   - notification mocks (now assert the tenant_slug kwarg)
#
# To deselect a genuinely pre-existing failure, add its pytest node id (or
# file / file::Class) on its own line below, with a comment explaining why and a
# plan to fix it. '#' = comment. Keep this file shrinking toward empty.
# ─────────────────────────────────────────────────────────────────────────────
