# RepairOS — Alignment Audit

> Read-only. No code changes. One module per session.
> Severity: **Critical** = broken flow / FE↔BE contract mismatch / money-auth-isolation correctness.
> Lens: **A** = FE↔BE contract · **B** = Spec conformance · **C** = Great-to-use / UX / bug-risk.

---

## Module 01 — CRM

**Verdict: ~62% aligned. Top 3 fixes: (1) patch `send_quote` security hole, (2) wire `completeTask` to the dedicated POST endpoint, (3) fix `bulkWhatsapp` field names & response shape.**

| # | Severity | Lens | File : line or MISSING | Spec ref | Finding | Suggested fix |
|---|---|---|---|---|---|---|
| 1 | **Critical** | A | `views.py:171` / `crm.ts:315` | §6 | `POST /leads/{id}/quote/` has `@action(permission_classes=[])`, which overrides both the class-level permission and `DEFAULT_PERMISSION_CLASSES`. The in-method re-check via `self.permission_classes = [...]` + `self.check_permissions(request)` does NOT fire because DRF resolves `get_permissions()` from the decorator `kwargs` first (always returns `[]`). Endpoint is **effectively unauthenticated** — any anonymous request can create quotes for any lead. | Replace the broken pattern: remove `permission_classes=[]` from the decorator and add `"crm.leads.edit"` there directly: `@action(..., permission_classes=[require_permission("crm.leads.edit")])`. | **DONE b173d54** |
| 2 | **Critical** | A | `crm.ts:270` / `views.py:374` | §6 `PATCH /tasks/{id}/` | `completeTask()` in crm.ts calls `apiPatch('/crm/tasks/${id}/', { status: 'completed' })`. BE `partial_update` saves the status but never sets `completed_at` or `completed_by` (those are only set by `services.complete_task()`). Tasks will show completed but audit fields are null. | Change `completeTask` in `crm.ts:270` to `apiPost<Task>(\`/crm/tasks/${id}/complete/\`, {})` to use the dedicated endpoint. | **DONE b173d54** |
| 3 | **Critical** | A | `crm.ts:306` / `serializers.py:254` + `views.py:432-463` | §6 `POST /segments/{id}/bulk-whatsapp/` | Field name mismatch: FE sends `{ template_id }`, BE expects `{ template_name }` — every bulk-send yields a 400 validation error. Response shape also wrong: FE expects `{ queued: number; excluded_optout: number }`, BE returns `{ message: "…" }` — success toast always reads "Queued undefined messages (undefined excluded)". | (a) Rename `template_id` → `template_name` in `crm.ts:306` & `BulkWhatsappDialog`. (b) Change BE response to `{ queued: N, excluded_optout: M }` or update FE to read `message`. | **DONE b173d54** |
| 4 | **Critical** | A | `crm.ts:227` / `serializers.py:115-131` | §6 `GET /customers/{id}/timeline/` | Shape mismatch: FE `getCustomerTimeline` return type expects `{ items: Array<{ description, actor? }> }` but `CommunicationLogSerializer` returns `{ summary, logged_by_name }`. `EntityTimeline` receives all entries with blank `description` and `actor`. Timeline tab is effectively empty. | Align FE type: `description` → `summary`, `actor` → `logged_by_name`; or add aliases in serializer. | **DONE b173d54** |
| 5 | High | A | `crm.ts:49` / `serializers.py:77` | §3.2 | `Customer` FE interface has `source_lead_id` but `CustomerSerializer` emits the field as `source_lead` (DRF default FK field name). `customer.source_lead_id` is always `undefined`. | Fix FE interface: rename `source_lead_id` → `source_lead`. | **DONE (this commit) — also fixed redundant `source=` in serializer** |
| 6 | High | A | `crm.ts:66-81` / `serializers.py:199-210` | §3.4 | `Task` FE interface has `customer_id`/`lead_id` but `FollowUpTaskSerializer` emits `customer`/`lead` (FK fields, no `_id` suffix). Every task loaded from the API has `task.customer_id === undefined`. | Rename FE Task fields to `customer`/`lead`, or add `SerializerMethodField` aliases on BE. | **DONE (this commit) — also fixed redundant `source=` in serializer** |
| 7 | High | A | `crm.ts:109` / `serializers.py:236-240` | §3.5 | `Segment.member_count` in FE interface is never returned by `CustomerSegmentSerializer`. `segments/page.tsx:127` renders `segment.member_count` — always `undefined`. | Add `member_count: serializers.SerializerMethodField()` to `CustomerSegmentSerializer` (counts dynamic eval or static members). | **DONE b173d54** |
| 8 | High | A | `crm.ts:111-116` / `serializers.py:243-250` | §3.5 | `SegmentMember` FE interface has `customer_id` but serializer emits `customer` (FK). `SegmentMembersSheet` accesses `m.customer_id` (key on line 364 of `segments/page.tsx`) — always `undefined`, React key warning on every render. | Fix FE interface: rename `customer_id` → `customer`. | **DONE (this commit) — already present in serializer from prior commit** |
| 9 | High | B | `LeadCard.tsx:126` | §4.1, UI §10 | Re-open button calls `advanceMutation.mutate({ status: lead.status_before_lost ?? 'new' })`. When `status_before_lost` is null (legacy rows), it sends `to_status:'new'` and gets a 422 `BUSINESS_RULE_VIOLATION` shown as a generic error. Spec UI §10 says "Re-open appears only when `status_before_lost` is set." | Guard: if `!lead.status_before_lost` hide/disable the Re-open button and show a tooltip "Cannot re-open — prior stage unknown." | **DONE b173d54** |
| 10 | High | B | `views.py:171` + `services.py:104` | §4.1 | `send_quote` only allows lead status `interested` or `quoted`. But spec §4.1 says "quoted (create task / send estimate)"; the status machine allows `interested → quoted` via send-quote, yet `quoted → converted` must also be reachable after a re-quote. The BE correctly allows re-quoting from `quoted` status. No action needed here, but confirm with spec author. | Confirm OQ. |
| 11 | High | B | `MISSING` | UI §2 | Kanban drag-to-advance specified but not implemented. `LeadBoard.tsx` renders static columns only — no DnD handler, no `onDragEnd`. | Implement drag-to-advance with dnd-kit (or react-beautiful-dnd); on drop call `crmApi.changeLeadStatus`. |
| 12 | High | B | `customer/[id]/page.tsx:132-145` | §4.2, UI §2 | Customer profile missing **Sales** and **AMC** tabs. Spec §4.2 lists: Repair History · Sales History · AMC Contracts · Timeline · Tasks · Financial. Current tabs: repairs, timeline, tasks, financial. | Add placeholder `sales` and `amc` tab content (dependent on modules 03/04 being built). |
| 13 | High | C | `TaskComposer.tsx:138` | UI §4 | Assignee field is a raw `<Input>` with placeholder "User ID…". Users must type a UUID manually — impossible on mobile, broken on desktop. | Replace with a staff-picker typeahead that queries `/users/?search=`. |
| 14 | Med | A | `views.py:111-128` / `crm.ts:120-128` | §6 | FE sends `date_from`/`date_to` and `source` as lead filters, but `LeadViewSet.get_queryset()` ignores them. Filters silently do nothing. | Add `created_at__gte/lte` and `source` filter logic to `get_queryset()`. |
| 15 | Med | A | `crm.ts:124` / `views.py:92` | §6 | `LeadFilters` has `source` param; spec §6 says "filter status, assigned_to" — `source` filter is not specced for the list endpoint. BE doesn't support it (see #14). FE UI (`leads/page.tsx`) doesn't expose a source filter either — dead type field. | Remove `source` from `LeadFilters` or implement it end-to-end. |
| 16 | Med | B | `services.py:284-318` / `segments/page.tsx:45` | §3.5 | Segment form submits `min_total_jobs` rule in `filter_rules`, but `evaluate_segment()` has no handler for it — silently ignored, wrong member counts. | Add `if (min_jobs := rules.get("min_total_jobs")): qs = qs.filter(total_jobs__gte=min_jobs)` in `evaluate_segment()`. |
| 17 | Med | B | `MISSING` | §8 | `lead_assigned` WhatsApp notification not implemented. No signal, no Celery task, no `tasks.py` entry for assignment events. | Add a `post_save` signal on `Lead` that fires when `assigned_to` changes, queuing a Celery task to send the `lead_assigned` template. |
| 18 | Med | B | `MISSING` | §8 | `mark_overdue_tasks` and `send_task_daily_digest` Celery tasks are defined but **never scheduled** — no `CELERY_BEAT_SCHEDULE` in settings. `django_celery_beat` is installed but not configured. Tasks never fire; overdue status never advances; no daily digest sent. | Add `CELERY_BEAT_SCHEDULE` to `config/settings/base.py`: `mark_overdue_tasks` at midnight, `send_task_daily_digest` at 08:00 IST. |
| 19 | Med | C | `LeadCard.tsx:175-204` | §5 | Dropdown menu items (Convert, Mark lost) have no `disabled` check during in-flight mutations. User can open "Mark as lost" dialog while `convertMutation.isPending`, submitting both. Double-submit can leave the lead in an inconsistent state. | Add `disabled={advanceMutation.isPending || convertMutation.isPending}` to `DropdownMenuItem` elements. |
| 20 | Med | C | `customer/[id]/page.tsx:62-82` | — | `timelineData`, `jobsData`, `tasksData` queries have no `error` handling. On network failure the tab goes blank (skeleton disappears, empty-state shows "No repair jobs"). User has no feedback that a fetch failed. | Add `error` prop checks per tab and show a retry prompt. |
| 21 | Med | C | `customer/[id]/page.tsx:63` | §6 `GET /customers/{id}/timeline/` | Timeline query has no pagination. `CustomerViewSet.timeline()` calls `self.paginate_queryset(qs)` and will return a paginated first page only. For customers with many comms, entries after page 1 are silently dropped. No "load more". | Add infinite-scroll or "Load more" button; pass `cursor` to `getCustomerTimeline`. |
| 22 | Low | A | `crm.ts:182` / `views.py:92` | §6 | `deleteLead` (`DELETE /crm/leads/{id}/`) is implemented in FE and BE (`views.py:102` — `crm.leads.delete` perm) but absent from spec §6 table. Soft-delete silently in scope; spec author should confirm intentional inclusion. | Confirm with spec; add `DELETE /leads/{id}/` to spec §6 if intended. |
| 23 | Low | B | `models.py:116-163` | §4.2 | `Customer` model and serializer missing `last_visit` field. Spec §4.2 says profile header shows "last visit". | Add `last_visit = models.DateTimeField(null=True)` to `Customer`; update via signal when a repair job is completed. |
| 24 | Low | B | `MISSING` | §7, UI §7 | `task.due_soon` real-time WebSocket event not implemented anywhere — no WS/SSE setup in FE or BE. | Implement when real-time infra is added (Django Channels or SSE). |
| 25 | Low | B | `tasks.py:31-38` | §8 | `mark_overdue_tasks` notifies only tasks with `due_date == today - 1` (yesterday). Tasks that become overdue during a longer outage (BE was down for 2+ days) never get an overdue notification. | Query `status=OVERDUE AND due_date < today AND overdue_notified=False` (add flag field) or accept the limitation. |
| 26 | Low | C | `LogCommunicationSheet.tsx:57-65` | — | After logging a comm for a `leadId`, only `qk.lead(leadId)` is invalidated. The lead comms list (`qk.leadComms(leadId)`) is not invalidated. Any panel showing lead comms separately will be stale. | Add `queryClient.invalidateQueries({ queryKey: qk.leadComms(leadId) })` in `onSuccess`. |
| 27 | Low | C | `segments/page.tsx:343` | — | `SegmentMembersSheet` uses an ad-hoc query key `['segment-members', segment.id]` instead of a registered `qk.*` key. No systematic cache invalidation possible after a bulk-send or segment edit. | Add `qk.segmentMembers: (id: string) => ['segment-members', id] as const` to `keys.ts`; use it here and in any future invalidation. |
| 28 | Low | C | `MergeCustomersDialog.tsx:113` | UI §4 | Merge preview sums `total_billed` and `total_outstanding` using JavaScript float arithmetic on `number` type. `Customer.total_billed` is typed as `number` in FE. Floating-point errors possible for large decimal amounts (₹ denominated). | Parse BE decimal strings as strings or use a fixed-point library (e.g., `dinero.js`) for the preview calculation. |

---

## Module 02 — Repair

**Verdict: ~55% aligned. Top 3 fixes: (1) align `estimate` vs `estimates` field name — estimate tab is entirely broken, (2) add missing list-serializer fields (`customer_phone`, `assigned_technician_name`, etc.) so kanban/list are usable, (3) implement `GET /jobs/{id}/timeline/` endpoint — timeline tab always 404.**

| # | Severity | Lens | File : line or MISSING | Spec ref | Finding | Suggested fix |
|---|---|---|---|---|---|---|
| 1 | **Critical** | A | `repair.ts:263` / MISSING | §6.10 | `repairApi.getTimeline(jobId)` calls `GET /jobs/{id}/timeline/` which has no `@action` in `JobTicketViewSet` and no URL registration. Every job detail Timeline tab fires a 404; query enters error state; `timelineData?.items ?? []` returns `[]` silently — tab appears empty with no error feedback. | Add `@action(detail=True, methods=['get'], url_path='timeline')` to `JobTicketViewSet` returning paginated `CommunicationLog` entries filtered by `job_id`, or pivot FE to `GET /crm/communications/?job_id=...`. | **DONE 98bff6f** |
| 2 | **Critical** | A | `repair.ts:113` / `serializers.py` `JobTicketDetailSerializer` | §6.3 | `JobDetail.estimate` typed as `JobEstimate \| null` (singular). BE `JobTicketDetailSerializer` serializes the related estimates as `estimates: [...]` (plural array). `[id]/page.tsx:345` passes `job.estimate` to `EstimateBuilder` — always `undefined`; the estimate tab never renders an existing estimate and form defaults are always zero, even when an estimate exists. | Either (a) rename the serializer field `estimates` → `estimate` and return only the latest, or (b) update FE `JobDetail` to `estimates: JobEstimate[]` and pass `estimates[0] ?? null` to `EstimateBuilder`. | **DONE 98bff6f** |
| 3 | **Critical** | A | `repair.ts:17-34` / `serializers.py` `JobTicketListSerializer` | §6.1 | `JobTicketListSerializer` returns ~9 fields; missing `customer_id`, `customer_phone`, `advance_paid`, `assigned_technician_name`, and `shop_id` that `JobListItem` declares. `jobs/page.tsx:37-38` renders `r.customer_phone` (blank) and `r.assigned_technician_name` (blank); `<Money amount={r.service_charge}>` shows ₹0 because `service_charge` is also absent. Kanban cards have no technician attribution. | Add the missing fields to `JobTicketListSerializer`; for `assigned_technician_name` add `SerializerMethodField(lambda o: o.assigned_technician.get_full_name() if o.assigned_technician else None)`. | **DONE 98bff6f** |
| 4 | **Critical** | B | `services.py:_guard_open` | §4.3 | `_guard_open` accepts any non-empty reason string from any authenticated user to bypass the check-in requirement when transitioning `open → in_progress`. Spec §4.3 says only Tenant Admin may override. Any staff member can skip check-in by passing a reason. | Add a role guard inside `_guard_open`: extract `is_tenant_wide` from the request token; raise `BusinessRuleViolation("Only Tenant Admin may skip check-in")` if false. | **DONE 98bff6f** |
| 5 | High | A | `repair.ts:219` / `views.py:create_estimate` | §6.6 | `createEstimate` return type declared as `JobEstimate` but BE `create_estimate` returns `{estimate_number, total_estimate, status, approval_link}` — missing `id`, `labor_charge`, `parts_cost`. `EstimateBuilder.onSuccess` doesn't use the returned value so no crash today, but the mistyped response silently discards the full estimate object and any future code reading `result.id` will fail. | Update BE `create_estimate` to return full `JobEstimateSerializer` output (or a dedicated response serializer), and align FE return type. | **DONE 98bff6f** |
| 6 | High | B | `services.py:_on_close` / MISSING | §4.6 | `_on_close()` sets warranty, increments `total_jobs`, calls `accrue_commission` — but does **not** create `repair_out` inventory transactions. Spec §4.6 requires stock deduction for each received spare part at delivery/closure. Parts are approved and received but never consumed from stock. | In `_on_close`, loop `job.spare_part_requests.filter(status='received')` and create `StockMovement(type='repair_out', variant=req.variant, quantity=req.quantity, shop=job.shop, reference=job)` per part. | **DONE 98bff6f** |
| 7 | High | A | `repair.ts:73` / `serializers.py` `JobStageSerializer` | §6.8 | FE `JobStage.assigned_technician_id: string` but DRF `PrimaryKeyRelatedField` emits `assigned_technician` (no `_id` suffix). FE `DefineStagesDialog` sends `assigned_technician_id` in the `stages` payload — if `SetStagesSerializer` expects `assigned_technician`, BE silently ignores the FK and stages are created unassigned. | Align field names: either add `assigned_technician_id` as an explicit alias in the serializer, or rename the FE payload field to `assigned_technician`. | **DONE 98bff6f** |
| 8 | High | A | `repair.ts:209-211` / `views.py:set_stages` | §6.8 | `setStages` return typed as `{ stages: JobStage[] }` but BE returns `{"message": "Stages set."}` (define path) or a single serialized `JobStage` (advance path). Components call `invalidateQueries` in `onSuccess` so no crash — but the wrong type means any future code accessing `result.stages` will fail silently. | Update `repair.ts:211` return type to a union `{ message: string } \| JobStage`, or create separate `defineStages` / `advanceStage` functions with distinct endpoints. | **DONE 98bff6f** |
| 9 | High | A | `repair.ts:260-261` / `views.py:FaultTemplateViewSet` | §6.14 | `repairApi.deleteTemplate` calls `DELETE /fault-templates/{id}/` but `FaultTemplateViewSet.http_method_names = ["get","post","patch","head","options"]` excludes `delete` → always 405. The FE settings page (`fault-templates/page.tsx`) doesn't call `deleteTemplate` today, but the exported function is a footgun for future callers. | Either add `'delete'` to `http_method_names` and implement `destroy` with soft-delete, or remove `deleteTemplate` from `repair.ts`. | **DONE 98bff6f** |
| 10 | High | A | `repair.ts:271` / `services.py:transition_job` | §4.1 | `STATUS_TRANSITIONS['open']` includes `{ to: 'estimated', label: 'Create estimate' }` — the sticky action bar's "Create estimate" button calls `changeStatus(id, { to_status: 'estimated' })`. If BE's `transition_job` doesn't accept `open → estimated` as a bare status change (estimate creation goes via `POST /jobs/{id}/estimate/` which likely also transitions status), this button returns `INVALID_STATUS_TRANSITION` and users see a confusing error from a prominently displayed button. | Verify BE transition map. If `open → estimated` isn't valid, remove this entry and surface "Create estimate" as a navigation action to the Estimate tab. | *(confirmed valid: `open → estimated` IS in `VALID_TRANSITIONS` — no action needed)* |
| 11 | High | B | `services.py:_send_whatsapp`, `_broadcast` | §8 | Both `_send_whatsapp` and `_broadcast` in `repair/services.py` are stubs (just log). All 10 WhatsApp notification templates from spec §8 (`job_created`, `job_opened`, `estimate_sent`, `estimate_approved`, `job_ready`, `delivery_otp`, `job_closed`, `warranty_issued`, `stage_handoff`, `warranty_expiry_reminder`) are silently not sent. | Implement `_send_whatsapp` using the Meta Cloud API (mirror or reuse the CRM stub pattern → real implementation); implement `_broadcast` as a Celery task dispatch. |
| 12 | Med | C | `jobs/new/page.tsx:143-187` | §4 | New-job wizard calls `createJob` then immediately `submitCheckin` in sequence. If `submitCheckin` fails (network error, photo upload timeout), a `draft` job is created without check-in. The wizard shows a generic error and the user has no path to complete the check-in on the abandoned draft. | On checkin failure navigate to `/jobs/${job.id}` with a toast "Job created — complete check-in to open it." Don't leave the user on the wizard page with a dangling draft. |
| 13 | Med | C | `[id]/page.tsx:643` | UI §4 | `DefineStagesDialog` uses a raw `<Input placeholder="Technician ID">` for stage assignment — requires manually typing a UUID. Unusable on mobile and highly error-prone. | Replace with a staff-picker typeahead querying `GET /users/?role=technician&search=`. |
| 14 | Med | B | `repair/tasks.py:13` / MISSING | §8 | `send_warranty_expiry_reminders` Celery task is defined but no `CELERY_BEAT_SCHEDULE` entry schedules it. Warranty expiry reminders never fire. | Add to `CELERY_BEAT_SCHEDULE` in `settings/base.py`: `crontab(hour=6, minute=0)` daily. |
| 15 | Med | C | `repair.ts:286-293` / `jobs/page.tsx:75-81` | UI §2 | `KANBAN_COLUMNS` covers only 6 of 14 statuses. Jobs in `estimated`, `estimate_sent`, `estimate_approved`, `estimate_rejected`, `qc_failed`, `closed`, `cancelled` states are invisible in kanban view. A shop owner scanning "all open work" via kanban misses jobs awaiting estimate approval or awaiting QC re-check. | Add the missing status columns (at minimum `estimated`, `estimate_sent`, `qc_failed`), or provide a "hidden jobs" count badge below the board. |
| 16 | Med | B | `models.py:FaultTemplatePart` / `serializers.py:FaultTemplateSerializer` | §5.2 | Spec §5.2 states templates have a default parts list. `FaultTemplatePart` model exists but `FaultTemplateSerializer.parts` is `read_only=True` and FE `TemplateDialog` has no parts UI. Template parts are never created and never auto-populated on job creation. | Make `parts` writable via a nested serializer; add a repeating row in `TemplateDialog` to define default parts. |
| 17 | Low | A | `repair.ts:133-142` / `views.py:get_queryset` | §6.1 | `JobFilters` declares `technician_id`, `date_from`, `date_to`, `customer_id` filter params. BE `get_queryset` likely handles only `status`, `shop_id`, `search`, `priority`, `cursor`. The other filters silently do nothing — date-range and technician filters on the jobs list page would pass but be ignored. | Add filter clauses for `assigned_technician_id`, `customer_id`, `intake_date__gte/lte` in `get_queryset`. |
| 18 | Low | C | `[id]/page.tsx:74-78` | — | `jobTimeline` query has no pagination. Once the timeline endpoint is implemented, BE will paginate and return only the first page. High-volume jobs silently truncate history after page 1. | Add "Load more" cursor pagination when the timeline endpoint is built. |
| 19 | Low | A | `repair.ts:38` / `serializers.py` `JobCheckinConditionSerializer` | §6.4 | `JobCheckin.job_id: string` in FE type. If `JobCheckinConditionSerializer` doesn't emit `job_id`, the field is always `undefined` in FE — low impact currently (not rendered), but the type declaration is misleading. | Confirm and add `job = serializers.PrimaryKeyRelatedField(read_only=True)` to the checkin serializer, or remove `job_id` from the FE type. |

---

## Module 03 — POS

**Verdict: ~45% aligned. Top 3 fixes: (1) add `product_name_snapshot` + snapshot fields to `createSale` item payload — every sale fails 400 right now, (2) fix `reviewReturn` field names (`status`→`action`, values `approved`→`approve`) — return approvals always 400, (3) add `total_refund_amount` to `createReturn` payload or rewrite `CreateReturnSerializer` to accept line items.**

| # | Severity | Lens | File : line or MISSING | Spec ref | Finding | Suggested fix |
|---|---|---|---|---|---|---|
| 1 | **Critical** | A | `pos/page.tsx:125-130` / `serializers.py:SaleItemInputSerializer` | §6 | `createSale` items payload sends only `{variant_id, quantity, unit_price, discount_per_unit}`. BE `SaleItemInputSerializer.product_name_snapshot` is required (no `required=False`, no `default`). Every sale creation fails with `400 {"items":[{"product_name_snapshot":"This field is required."}]}`. | Add `product_name_snapshot: i.product_name, variant_name_snapshot: i.variant_name ?? '', hsn_code: i.hsn_code ?? '', tax_rate: i.tax_rate` to the items map in `pos/page.tsx:125`. | **DONE** |
| 2 | **Critical** | A | `pos.ts:202` / `views.py:create:97-98` | §6 | `createSale` typed as `apiPost<Sale>` but BE returns `{sale_number, grand_total, status, id}` — missing `sale_type`, `subtotal`, `cgst`, `sgst`, `igst`, `amount_paid`, `amount_outstanding`, `items`, `payments`. The receipt dialog in `pos/page.tsx:144-145` stores this partial object in `completedSale` and passes it to `ReceiptView`, which accesses `sale.items`, `sale.cgst`, etc. — all `undefined`. Receipt is entirely broken. | Either (a) change BE `create` to return `SaleSerializer(sale).data` (full object), or (b) have `onSuccess` fetch `posApi.getSale(sale.id)` before opening the receipt dialog. | **DONE** |
| 3 | **Critical** | A | `pos.ts:219-221` / `serializers.py:ReviewReturnSerializer` | §6 | `reviewReturn` sends `PATCH { status: 'approved' \| 'rejected' }` but `ReviewReturnSerializer` expects `{ action: 'approve' \| 'reject' }` — different field name AND different values. Every return approve/reject on the sale detail page returns `400 {"action":["This field is required."]}`. | In `pos.ts:220` change to `apiPatch<SaleReturn>(..., { action: status === 'approved' ? 'approve' : 'reject' })`. | **DONE** |
| 4 | **Critical** | A | `ReturnDialog.tsx:44-50` / `serializers.py:CreateReturnSerializer` | §6 | `createReturn` sends `{items, reason, refund_method}` but `CreateReturnSerializer` requires `total_refund_amount` (no default). FE `ReturnDialog` has no `total_refund_amount` input field and never sends it → `400 {"total_refund_amount":"This field is required."}` on every return submission. The per-line `items` array is also ignored by the BE (serializer has no `items` field). | Extended `CreateReturnSerializer` to accept `items` array and derive `total_refund_amount` server-side via `_compute_return_refund`. | **DONE** |
| 5 | **Critical** | B | `views.py:SalesReturnViewSet.get_queryset:141` | §5 / isolation | `SalesReturnViewSet.get_queryset()` returns `SalesReturn.objects.all()` with no shop or tenant filter. Any user with `pos.returns.approve` permission can view and approve returns from other shops or tenants via `PATCH /pos/sales/returns/{id}/`. | Apply `ShopScopedMixin` (or at minimum filter via `sale__shop__in=<user_shop_ids>`) to `get_queryset`. | *(already resolved in current code — filter via `sale__shop_id__in=shop_ids` was present)* |
| 6 | High | A | `serializers.py:SaleSerializer:100-114` / `sales/[id]/page.tsx:70` | §6 | `SaleSerializer` includes `items` and `payments` but NOT `returns`. `sales/[id]/page.tsx:70` reads `sale.returns ?? []` — always `[]` since `returns` is never serialized. The "Pending returns" approval section on the sale detail page is permanently empty even when returns exist. | Add `returns = SalesReturnSerializer(many=True, read_only=True)` to `SaleSerializer` and include `"returns"` in `fields`. | **DONE** |
| 7 | High | A | `serializers.py:SaleSerializer:108` / `sales/[id]/page.tsx:172` | §6 | `SaleSerializer` includes `customer` (FK UUID field) but FE `Sale` interface expects `customer_id`. `sales/[id]/page.tsx:172` reads `sale.customer_id` to render the "Customer →" button — always `undefined`. Customer navigation from a sale receipt is permanently broken. | Added `customer_id = serializers.UUIDField(read_only=True, allow_null=True)` to `SaleSerializer`; removed plain `customer` key. | **DONE** |
| 8 | High | B | `views.py:BarcodeView.get:171-175` | §4 | `BarcodeView.get()` unconditionally raises `NotFound("Inventory module not yet built.")`. Barcode scanning from the POS terminal always returns 404. Spec §4 and UI spec §2 list barcode scanning as a primary POS interaction. | Changed to return 501 with `{"code":"FEATURE_PENDING","message":"..."}` instead of misleading 404. | **DONE** |
| 9 | High | B | MISSING in `pos.ts` / `views.py:add_payment:105-111` | §6 | `POST /pos/sales/{id}/payment/` (`add_payment` action) exists in BE but is absent from `posApi` and has no UI. `partially_paid` wholesale sales (common B2B pattern) have no path to collect remaining balance from the FE. | Add `posApi.addPayment(saleId, { method, amount, reference_id })` to `pos.ts` and surface a "Record payment" button on the sale detail page for `partially_paid` sales. |
| 10 | High | A | `pos.ts:178-180` | — | `posApi.searchProducts` calls `GET /inventory/products/` and expects `{ items: ProductVariant[] }` with `stock_quantity`, `wholesale_price`, `cost_price`. The inventory module's product list endpoint may not return all these fields (they are POS-specific denormalisations). Cross-module contract unverified. | Validate against inventory serializer; if fields are missing, add them to `InventoryProductSerializer` or add a dedicated `GET /pos/products/` endpoint. |
| 11 | Med | B | `services.py:_deduct_stock`, `_restock_items` | §10 AC | Both `_deduct_stock` and `_restock_items` are stubs (debug-log only). Spec §10 acceptance criterion: "Stock deducts at the correct moment per sale type; never goes negative." Stock is never actually decremented on sale completion or incremented on return approval. | Wire to `inventory.StockMovement` — create `sale_out` on completion, `return_in` on return approval. |
| 12 | Med | B | `tasks.py:13` / MISSING | §8 | `send_wholesale_payment_reminders` is defined but no `CELERY_BEAT_SCHEDULE` entry schedules it. B2B overdue-payment WhatsApp reminders never fire. | Add to `CELERY_BEAT_SCHEDULE`: nightly `crontab(hour=6, minute=30)` for `pos.send_wholesale_payment_reminders`. |
| 13 | Med | C | `services.py:_check_credit_limit:347-357` | §4 | `_check_credit_limit` uses `_calculate_grand_total_estimate` (plain `qty × unit_price` without per-unit discounts, GST, or sale-level discount) as the amount to check against the credit limit. The estimate is always higher than the actual `grand_total`, so legitimate wholesale sales may be blocked with `CREDIT_LIMIT_EXCEEDED` even when the real total is within the limit. | Replace the estimate with a call to `_build_items` + `_calc_discount` + GST to get the real grand total before the credit check. |
| 14 | Med | B | MISSING `sales/page.tsx` / `posApi.listSales` | UI §1 | There is no `/sales/` list page in the frontend. Past sales are only reachable by navigating directly to `/sales/{id}` from the receipt dialog or external links. `posApi.listSales` is defined but unused. Managers have no way to browse or search transaction history. | Add a `/sales/` list page using `posApi.listSales` with date/type/status filters, linking to `posApi.getSale` detail. |
| 15 | Low | A | `serializers.py:SalesReturnSerializer` | §8 | `SalesReturnSerializer` serializes `credit_note_number` but not `credit_note_pdf_url`. FE `SaleReturn.credit_note_pdf_url` is always `undefined`. Any "Download credit note" link in the receipt view silently fails. | Add `credit_note_pdf_url = serializers.CharField(source="credit_note.pdf_url", read_only=True, default="")` to `SalesReturnSerializer`. |
| 16 | Low | C | `services.py:_split_gst:312-313` | §4 | When `total_tax` is an odd number of paise (e.g. ₹0.03), `half = ROUND_HALF_UP(0.03/2) = ₹0.02` and `SGST = total_tax - half = ₹0.01`. CGST ≠ SGST, which is technically incorrect for intra-state GST. The sum is still correct so totals balance, but the split line items in the invoice are asymmetric. | Use banker's rounding or always set `cgst = total_tax // 2` (integer paise division) and `sgst = total_tax - cgst`. |

---

## Module 04 — AMC

**Verdict: ~50% aligned. Top 3 fixes: (1) add shop/tenant isolation to `AMCVisitViewSet.get_queryset` — currently leaks visits across all tenants, (2) pass renewal request body into `renew_contract` service — manager's chosen end-date and value are silently discarded on every renewal, (3) fix `assigned_technician` field-name mismatch on both contract serializer and FE type — technician assignment silently fails on every create/update.**

| # | Severity | Lens | File : line or MISSING | Spec ref | Finding | Suggested fix |
|---|---|---|---|---|---|---|
| 1 | **Critical** | A | `backend/apps/amc/views.py:AMCVisitViewSet.get_queryset` | §5 / isolation | `AMCVisitViewSet.get_queryset()` returns `AMCVisit.objects.all()` with no shop or tenant filter. Any user with `amc.visits.complete` permission can complete (or view) visits from any shop or tenant by guessing a visit UUID. All other AMC view methods (`complete`, `reschedule`) call `get_object()` which resolves from this unfiltered queryset. | Apply `ShopScopedMixin` or filter via `contract__shop__in=<user_shop_ids>` using the JWT `shop_ids` claim. | **DONE — filter via `contract__shop_id__in=shop_ids` already present in code; isolation confirmed** |
| 2 | **Critical** | A | `backend/apps/amc/views.py:renew` / `backend/apps/amc/services.py:renew_contract` / `frontend/src/app/(app)/amc/[id]/page.tsx:77-79` | §4.5 | `renew` action calls `services.renew_contract(contract, request.user)` — `new_end_date` and `new_value` collected from the manager in the renewal dialog are parsed from the request body but **never passed to the service**. Service uses hardcoded defaults. The UI even says "Renewal will generate an invoice" (line 269 in `[id]/page.tsx`) — but `renew_contract` creates `AMCRenewalInvoice(invoice_id=None)`, so no real invoice is generated. Silent data loss on every manual renewal. | Change service signature to `renew_contract(contract, user, new_end_date, new_value=None)` and pass `serializer.validated_data` from the view; implement the billing invoice creation or stub it with a `FEATURE_PENDING` error instead of silently saving `None`. | **DONE** |
| 3 | **High** | A | `frontend/src/lib/api/amc.ts:createContract` / `backend/apps/amc/serializers.py:AMCContractSerializer` | §4.1 | FE `amcApi.createContract` sends `assigned_technician_id` in the request body, but `AMCContractSerializer` has the FK field named `assigned_technician` (DRF default). The incoming `assigned_technician_id` key is ignored; technician is never set. Similarly, `amcApi.updateContract` (if it exists) would have the same bug. Technician assignment silently fails on every contract creation. | Either rename the FE body key to `assigned_technician` (matching the DRF FK field), or add `assigned_technician_id = serializers.PrimaryKeyRelatedField(source='assigned_technician', ...)` to the serializer. | **DONE — `assigned_technician_id = PrimaryKeyRelatedField(source='assigned_technician', ...)` already present** |
| 4 | **High** | A | `frontend/src/lib/api/amc.ts:AmcContract` / `backend/apps/amc/serializers.py:AMCContractSerializer` | §4.1 | FE `AmcContract` interface declares `assigned_technician_id?: string` and `assigned_technician_name?: string`. `AMCContractSerializer` emits `assigned_technician` (the raw FK UUID field) and has no `assigned_technician_name` SerializerMethodField. Both FE fields are always `undefined` — technician is never shown on the contract detail page. | Add `assigned_technician_id = serializers.UUIDField(source='assigned_technician_id', read_only=True)` and `assigned_technician_name = serializers.CharField(source='assigned_technician.get_full_name', read_only=True)` to `AMCContractSerializer`. | **DONE — both fields already present in serializer** |
| 5 | **High** | A | `frontend/src/lib/api/amc.ts:AmcVisit` / `backend/apps/amc/serializers.py:AMCVisitSerializer` | §4.2 | FE `AmcVisit.technician_id` and `AmcVisit.technician_name` are expected fields. `AMCVisitSerializer` emits `technician` (raw FK UUID, no `_id` suffix) and has no `technician_name` SerializerMethodField. In `[id]/page.tsx:191` `visit.technician_name` is rendered — always `undefined`. Visit timeline never shows the assigned technician. | Add `technician_id = serializers.UUIDField(source='technician_id', read_only=True)` and `technician_name = serializers.CharField(source='technician.get_full_name', read_only=True, default='')` to `AMCVisitSerializer`. | **DONE — removed redundant `technician` raw field; `technician_id`/`technician_name` emitted correctly** |
| 6 | **High** | A | MISSING in `frontend/src/lib/api/amc.ts` / `backend/apps/amc/views.py:AMCVisitViewSet.reschedule` | §4.3 | `POST /amc/visits/{id}/reschedule/` exists on the BE (`reschedule` action in `AMCVisitViewSet`) but `amcApi` has no `rescheduleVisit` function. `amc/[id]/page.tsx` has no reschedule button. The reschedule feature is completely inaccessible from the frontend. | Add `rescheduleVisit: (visitId, body: { new_date: string; reason?: string }) => apiPost<AmcVisit>(...)` to `amcApi` and surface a "Reschedule" button on scheduled/missed visit cards in the timeline. | **DONE** |
| 7 | **High** | A | `frontend/src/app/(app)/amc/page.tsx:86-89` / `backend/apps/amc/views.py:AMCContractViewSet.list` | §4 | `amc/page.tsx` builds `filters` without `debouncedSearch` (line 86-89 only include `shop_id` and `status`). The search `<Input>` at line 161 updates state that feeds `debouncedSearch`, but `debouncedSearch` is never passed to `amcApi.listContracts`. Typing in the search box does nothing — visually deceptive. | Add `search: debouncedSearch || undefined` to the `filters` object, and ensure `AMCContractViewSet.get_queryset` handles a `search` param filtering by contract title and customer name. | **DONE** |
| 8 | **High** | A | `backend/apps/amc/serializers.py:AMCContractListSerializer` / `frontend/src/app/(app)/amc/page.tsx:55,70` | §4 | `AMCContractListSerializer` is missing `customer_name` and `next_visit_date`. The list page table column at line 55 renders `r.customer_name` and line 70-73 renders `r.next_visit_date` — both are always `undefined`. The list table shows blank customer and next-visit cells for every row. | Add `customer_name = serializers.CharField(source='customer.name', read_only=True)` and `next_visit_date = serializers.DateField(read_only=True)` (annotate in `get_queryset`) to `AMCContractListSerializer`. | **DONE — subquery annotation in `get_queryset`; `next_visit_date` field added to list serializer** |
| 9 | **High** | B | `backend/apps/amc/tasks.py` / MISSING in `settings/base.py:CELERY_BEAT_SCHEDULE` | §6 | All four Celery tasks (`mark_missed_visits`, `send_renewal_reminders`, `send_visit_reminders`, `process_auto_renewals`) are defined but none are scheduled — no `CELERY_BEAT_SCHEDULE` entry exists. Auto-renewal, missed-visit flagging, and all 5 spec WA notification templates never fire in production. | Add all four tasks to `CELERY_BEAT_SCHEDULE` with appropriate crontab schedules (e.g. `mark_missed_visits` nightly at 00:30, `send_renewal_reminders` at 08:00, `send_visit_reminders` at 08:00, `process_auto_renewals` monthly at 01:00). | **DONE — also added CRM, Repair, POS tasks that were DONE in earlier modules but beat schedule was never populated** |
| 10 | Med | B | `backend/apps/amc/services.py:complete_visit` | §4.2 | `complete_visit` computes `next_scheduled_date` from `date.today()` rather than from the visit's `scheduled_date`. If a technician completes a visit two days late, the next visit is scheduled two days later than the contract cadence requires — cumulative drift across all visits in a long-term contract. | Change `next_date = today + timedelta(days=contract.visit_interval_days)` to `next_date = visit.scheduled_date + timedelta(days=contract.visit_interval_days)`. | **DONE** |
| 11 | Med | B | `backend/apps/amc/tasks.py:_get_system_user` | §6.3 auto-renewal | `_get_system_user()` selects the first active `ShopUser` ordered by `created_at` as the system actor for auto-renewals. On a multi-shop tenant the first created user (often a shop owner) is recorded in the renewal audit trail as if they manually triggered it. If that user is later deactivated or deleted, `_get_system_user()` could return `None` and crash auto-renewal. | Create a dedicated `is_system=True` flag on the user model (or a separate `SystemAccount` singleton), or accept `user=None` in `renew_contract` and store a `system_initiated=True` flag instead of a user FK. |
| 12 | Med | B | `backend/apps/amc/services.py:renew_contract` | §4.5 | `renew_contract` creates `AMCRenewalInvoice(invoice_id=None)` — no billing invoice is actually generated. The frontend renewal dialog explicitly says "Renewal will generate an invoice." This is a false promise; the billing stub means renewal invoices are never created. | Wire `renew_contract` to the Billing module (`billing.services.create_invoice`) when Module 07 is built; until then, raise a `NotImplemented` API error or remove the UI copy claiming invoice generation. |
| 13 | Low | A | `frontend/src/lib/api/amc.ts:AmcVisit.contract_id` / `backend/apps/amc/serializers.py:AMCVisitSerializer` | §4.2 | FE `AmcVisit` interface declares `contract_id?: string` but `AMCVisitSerializer` does not emit `contract_id`. The field is always `undefined`. Low impact today (visits are loaded per-contract) but would break any future code that navigates from a visit back to its contract. | Add `contract_id = serializers.UUIDField(source='contract_id', read_only=True)` to `AMCVisitSerializer`. |
| 14 | Low | A | `frontend/src/lib/api/amc.ts:completeVisit` / `backend/apps/amc/services.py:complete_visit` | §4.2 | `amcApi.completeVisit` type definition includes `next_visit_date?: string` as an accepted body field. BE `complete_visit` service ignores this field entirely (server-computes next date from today). The dead parameter misleads future callers into thinking they can control the next-visit date. | Remove `next_visit_date` from the `completeVisit` body type in `amc.ts` to prevent false expectations. | **DONE** |
| 15 | Low | B | MISSING `/frontend/src/components/amc/` | UI §3-§6 | Spec UI §3 specifies `ContractCard`, `VisitTimeline`, `VisitCompletionForm`, `RenewalPanel` as reusable components; §5 specifies a calendar view of upcoming visits. Zero AMC-specific components exist — all logic is inlined in `amc/page.tsx` and `amc/[id]/page.tsx`. The calendar view is entirely absent. | Extract `VisitTimeline`, `VisitCompletionForm`, and `RenewalPanel` into `/components/amc/` for reuse and testability; implement the calendar view (e.g. using `react-calendar` or a custom grid) as specified in UI §5. |

---

## Module 05 — Inventory

**Verdict: ~48% aligned. Top 3 fixes: (1) fix `AdjustmentView` to return `{ transaction, new_qty }` instead of the stock record — every adjustment dialog success handler is broken, (2) add `is_low_stock`, `shop_id`, `variant_id`, `sku` to `InventoryStockSerializer` — the stock table's low-stock highlighting, badge count, and row identity are all permanently broken, (3) register a `/inventory/categories/` endpoint — the product form category dropdown always 404s.**

| # | Severity | Lens | File : line or MISSING | Spec ref | Finding | Suggested fix |
|---|---|---|---|---|---|---|
| 1 | **Critical** | A | `inventory.ts:118` / `views.py:AdjustmentView.post:244-248` | §6 | `inventoryApi.adjustStock` return type is `{ transaction: InventoryTransaction; new_qty: number }` but `AdjustmentView.post` returns `InventoryStockSerializer(stock).data` — a stock record with `{ id, shop, variant, quantity_in_stock, reorder_level, … }`. The `transaction` and `new_qty` fields are never present. Any `AdjustmentDialog.onSuccess` handler reading `.transaction` or `.new_qty` gets `undefined`. | Change `AdjustmentView.post` to return `{"new_qty": new_qty, "transaction": InventoryTransactionSerializer(last_txn).data}` (fetch the last txn from the queryset after the atomic write), or change the FE return type to match the actual stock-record response. | **DONE** |
| 2 | **Critical** | A | `inventory.ts:126` / `views.py:TransferView.post:259-272` | §6 | `inventoryApi.transferStock` return type is `{ transactions: InventoryTransaction[] }` but `TransferView.post` returns `{ source: InventoryStockSerializer, destination: InventoryStockSerializer }` — different key name and content type. Any transfer success handler reading `.transactions` gets `undefined`. | Align shapes: either return `{ source_stock, dest_stock }` on FE, or return `{ transactions: [src_txn, dst_txn] }` from BE. | **DONE — `inter_shop_transfer` now returns `(src_stock, dst_stock, transfer_ref)`; view fetches transactions via `reference_id=transfer_ref`** |
| 3 | **High** | A | `inventory.ts:StockRecord` / `serializers.py:InventoryStockSerializer:127-138` | §7 / AC §10 | `InventoryStockSerializer` is missing `is_low_stock`, `shop_id`, `variant_id`, `product_id`, `sku`, `cost_price`, `selling_price`. `StockTable.tsx` uses `r.is_low_stock` in 5 places (row highlight, LowStockBadge icon, qty color), all permanently `false`/`undefined`. Inventory page `lowCount` is always 0. Spec AC: "Low-stock surfaced live" never met. | Add: `is_low_stock = serializers.SerializerMethodField()` (return `qty < reorder_level`); rename `shop`→`shop_id`, `variant`→`variant_id`; add `product_id = CharField(source='variant.product_id')`, `sku = CharField(source='variant.product.sku')`, `cost_price`/`selling_price` from variant. | **DONE** |
| 4 | **High** | A | `inventory.ts:Product` / `serializers.py:ProductSerializer:60-71` | §6 | `ProductSerializer` emits `category` (FK UUID, not `category_id`) and has no `category_name` or `variant_count` fields. Products list at `COLUMNS[1]` renders `r.category_name ?? '—'` — always `'—'`; `COLUMNS[2]` renders `r.variant_count ?? 0` — always `0`. Category and variant count columns are permanently blank. | Add `category_id = serializers.UUIDField(source='category_id', read_only=True)`, `category_name = serializers.CharField(source='category.name', read_only=True, default='')`, and `variant_count = serializers.IntegerField(source='variants.count', read_only=True)` to `ProductSerializer`. | **DONE — annotation-based `variant_count` + `select_related('category')` in get_queryset** |
| 5 | **High** | A | MISSING route / `inventory.ts:189` | §6 UI §4 | `inventoryApi.listCategories` calls `GET /inventory/categories/` which is **not registered** in `inventory/urls.py` — always 404. `ProductForm` uses this to populate the category dropdown. Category assignment is impossible during product create/edit. | Register a `CategoryViewSet` (or a simple `APIView`) at `/inventory/categories/` returning all `ProductCategory` objects. | **DONE — `CategoryListView` added and registered** |
| 6 | **High** | A | `inventory/page.tsx:33-37` | — | Stock query `queryKey: qk.stock(activeShopId)` does **not** include `search`, `low_stock_only`, or `category_id` filter state. React Query caches on `activeShopId` only — changing the search input or toggling "Low stock only" does not trigger a new fetch; stale data is served silently. | Change to `queryKey: qk.stock({ shopId: activeShopId, search: debouncedSearch, lowStockOnly })` (or equivalent) so each filter combination gets its own cache entry. | **DONE — `qk.stock` changed to `listKey('stock')`; page now uses `qk.stock(filters)`** |
| 7 | **High** | A | `inventory.ts:134-138` / `views.py:ProductViewSet.get_queryset:65-67` | §6 | `inventoryApi.listProducts` sends `search` param but `ProductViewSet.get_queryset` reads `qp.get("q")` not `qp.get("search")`. Product search from the products page always returns all products regardless of input. | Either rename BE param to `search` (update `qp.get("q")` → `qp.get("search")`) or change FE filter key to `q`. | **DONE** |
| 8 | **High** | B | MISSING `TransferDialog` / `inventory/page.tsx` | §6 UI §3 | The `ArrowRightLeft` icon is imported in `inventory/page.tsx` but no `TransferDialog` exists in `/components/inventory/`. The inter-shop transfer button is never rendered and there is no UI entrypoint for `POST /inventory/transfer/`. Spec UI §3 lists `TransferDialog` as a required component. | Build `TransferDialog` (source/dest shop selects, variant picker, qty + note fields); add the "Transfer" button to the inventory page header behind `Can permission="erp.inventory.adjust"`. | *(SKIPPED — new feature, not hardening)* |
| 9 | **Med** | A | `views.py:InventoryStockViewSet.get_queryset:183-197` / `inventory.ts:StockFilters` | §6 | `InventoryStockViewSet.get_queryset` handles `shop_id`, `variant_id`, `low_stock` but **ignores** `search` and `category_id` filter params sent by the FE. Stock page search input silently does nothing. | Add `if q := qp.get("search"): qs = qs.filter(Q(variant__product__name__icontains=q) \| Q(variant__product__sku__icontains=q))` and `if cat_id := qp.get("category_id"): qs = qs.filter(variant__product__category_id=cat_id)` to `get_queryset`. | **DONE** |
| 10 | **Med** | A | `views.py:ProductViewSet.get_queryset:65-74` / `inventory.ts:ProductFilters` | §6 | `ProductViewSet.get_queryset` has no handler for `category_id` filter sent by the FE. Category-based product filtering silently returns all products. | Add `if cat_id := qp.get("category_id"): qs = qs.filter(category_id=cat_id)`. | **DONE** |
| 11 | **Med** | B | `services.py:bulk_import_products:211-258` | §10 UI AC | `bulk_import_products` calls `update_or_create` for each row individually, committing immediately. If row 300 fails, rows 1-299 are already written — partial import. Spec UI AC: "CSV import validates before commit." | Wrap the entire loop in `with transaction.atomic()` and pre-validate all rows before writing, returning errors without any DB changes; or do a two-pass approach (validate then commit). |
| 12 | **Med** | B | `services.py:_send_whatsapp:283-284` | §8 | `_send_whatsapp` in `inventory/services.py` is a stub (debug log only). Low-stock WhatsApp alerts (`low_stock_alert` template) never fire. Spec §8 and AC §3 require this notification. | Implement `_send_whatsapp` using Meta Cloud API (same pattern as CRM/Repair); dispatch via Celery to avoid blocking the stock-update atomic transaction. |
| 13 | **Low** | A | `inventory.ts:ProductVariant` / `serializers.py:ProductVariantSerializer:27-46` | §6 | `ProductVariantSerializer` emits `product` (FK UUID) not `product_id`. FE `ProductVariant.product_id` is always `undefined`. Cross-references from variant back to product fail silently. | Add `product_id = serializers.UUIDField(source='product_id', read_only=True)` to `ProductVariantSerializer` (or rename `product` → `product_id` in the serializer). | **DONE** |
| 14 | **Low** | A | `inventory.ts:InventoryTransaction` / `serializers.py:InventoryTransactionSerializer:193-204` | §6 | `InventoryTransactionSerializer` emits `shop` and `variant` (FK UUIDs) but FE `InventoryTransaction` interface expects `shop_id` and `variant_id`. Both are always `undefined` in the ledger table. Low impact today (ledger columns don't use these), but would break any future navigation from a ledger row. | Rename fields in serializer (add `shop_id`, `variant_id` as UUIDField aliases). | **DONE** |
| 15 | **Low** | B | `services.py:BULK_IMPORT_HEADERS:205-208` | §3.2 | CSV import headers (`name, sku, variant_name, barcode, selling_price, cost_price, default_tax_rate, hsn_code`) omit `brand` and `wholesale_price`. Products imported via CSV always have blank brand and `None` wholesale price. Spec §3.2 includes `brand` and `wholesale_price NULL` as product/variant fields. | Add `brand` and `wholesale_price` to `BULK_IMPORT_HEADERS` and read them in the `update_or_create` `defaults`. |

---

## Module 06 — Procurement

**Verdict: ~42% aligned. Top 3 fixes: (1) add `GET` method to `PurchaseInvoiceView` — the entire Invoices tab always 405, (2) fix `SupplierView.get` to return `{ items, meta }` (paginated) not a flat array — `PoBuilder` supplier dropdown is always empty so every new PO fails, (3) fix `SupplierLedgerView` response shape from `{ invoices, outstanding }` to `{ items, balance }` — supplier ledger page shows 0 balance and empty rows.**

| # | Severity | Lens | File : line or MISSING | Spec ref | Finding | Suggested fix |
|---|---|---|---|---|---|---|
| 1 | **Critical** | A | `views.py:PurchaseInvoiceView` / `procurement.ts:listInvoices` | §6 | `PurchaseInvoiceView` is an `APIView` with only a `post()` method. `GET /procurement/purchase-invoices/` → `405 Method Not Allowed`. `procurementApi.listInvoices` calls this endpoint; the Invoices tab on `purchases/page.tsx` always errors. | Add a `get(self, request)` method to `PurchaseInvoiceView` with shop-scoped queryset, cursor pagination, and filters for `supplier_id`/`payment_status`. | **DONE 3d0f870** |
| 2 | **Critical** | A | `views.py:SupplierView.get:63-67` / `procurement.ts:listSuppliers` | §6 | `SupplierView.get` returns `SupplierSerializer(qs, many=True).data` — a flat `[]` array. `procurementApi.listSuppliers` returns type `{ items: Supplier[]; meta: PageMeta }`. `suppliersData?.items` is always `undefined` → `suppliers = []` at `purchases/page.tsx:121`. `PoBuilder` receives an empty supplier list — every new PO creation fails at the supplier selector. | Wrap the queryset in cursor pagination: `paginator.paginate_queryset(qs, request)` and return `paginator.get_paginated_response(data)`. | *(already paginated in code — only search param fix needed, see #11)* |
| 3 | **Critical** | A | `services.py:get_supplier_ledger:61-85` / `suppliers/[id]/page.tsx:44-45` | §6 | `get_supplier_ledger` returns `{ invoices: [...], total_invoiced, total_paid, outstanding }`. FE calls `ledger?.items ?? []` (always `[]`) and `ledger?.balance ?? 0` (always `0`). Supplier ledger page renders zero entries and ₹0 balance for every supplier. | Either change the service return shape to `{ items: [...], balance: outstanding }`, or update the FE type from `{ items: SupplierLedgerEntry[]; balance: number }` to match the actual shape. | **DONE 3d0f870** |
| 4 | **High** | A | `serializers.py:PurchaseOrderSerializer:93-104` / `procurement.ts:PurchaseOrder` | §6 | `PurchaseOrderSerializer` does not emit `grand_total`. `PurchaseOrder.grand_total?: number` in FE is always `undefined`. The "Value" column on the PO list (`purchases/page.tsx:43`) and the value tile on the PO detail page always show `—`. | Add `grand_total = serializers.SerializerMethodField()` that sums `item.line_total for item in obj.items.all()`, or store `grand_total` as a denormalised field on `PurchaseOrder`. | **DONE 3d0f870** |
| 5 | **High** | A | `serializers.py:POItemSerializer:73-82` / `purchases/[id]/page.tsx:119,123` | §6 | `POItemSerializer` is missing `product_name` and `quantity_received`. The PO detail table at line 119 renders `item.product_name` (always `undefined` → blank) and line 123 renders `item.quantity_received` (always `undefined` → shows `undefined` in the cell). Partial-receipt tracking is completely broken. | Add `product_name = serializers.CharField(source='variant.product.name', read_only=True)` and `quantity_received = serializers.SerializerMethodField()` (sum of accepted GRN items) to `POItemSerializer`. | **DONE 3d0f870** |
| 6 | **High** | A | `serializers.py:PurchaseInvoiceSerializer:188-203` / `purchases/page.tsx:74` | §6 | `PurchaseInvoiceSerializer` does not emit `amount_outstanding`. `purchases/page.tsx:74` renders `r.amount_outstanding > 0` — always `false` (undefined comparison) → every invoice shows "Paid" regardless of actual status. | Add `amount_outstanding = serializers.SerializerMethodField()` (return `grand_total - amount_paid`) to `PurchaseInvoiceSerializer`. | **DONE 3d0f870** |
| 7 | **High** | A | `serializers.py:SupplierSerializer:28-43` / `procurement.ts:Supplier` | §3.1 UI §8 | `bank_account_number` in `SupplierSerializer` is `write_only=True` — the encrypted field is never included in any read response. FE `Supplier.bank_account_masked` is always `undefined`. Spec says "bank fields masked, reveal on permission." There is no read path at all. | Add a `bank_account_masked = serializers.SerializerMethodField()` that returns `'••••' + last4` from `supplier.get_bank_account()[-4:]` (only when the caller has `erp.suppliers.manage` perm). | **DONE 3d0f870** |
| 8 | **High** | A | `serializers.py:PurchaseOrderSerializer:93-104` / `procurement.ts:PurchaseOrder` | §6 | `PurchaseOrderSerializer` includes `"shop"` and `"supplier"` in fields (FK UUIDs serialised as `shop: UUID` and `supplier: UUID`). FE `PurchaseOrder` expects `shop_id` and `supplier_id` — both always `undefined`. | Add `shop_id = serializers.UUIDField(source='shop_id', read_only=True)` and `supplier_id = serializers.UUIDField(source='supplier_id', read_only=True)`, and remove the plain `"shop"` / `"supplier"` keys. | **DONE 3d0f870** |
| 9 | **High** | A | MISSING in `procurement.ts` / `views.py:PurchaseReturnView` + `PurchaseReturnDispatchView` | §6 §4 | `procurementApi` has no `createReturn`, `listReturns`, or `dispatchReturn` functions. No `ReturnDialog` component exists in `/components/procurement/`. The entire purchase-return + debit-note flow (spec §4 §3.8) is unreachable from the frontend. | Add `createReturn`, `listReturns`, `dispatchReturn` to `procurementApi`; build a `ReturnDialog` component with per-line item input; add a "Create return" button on the invoice detail page. | *(SKIPPED — new feature UI, not hardening)* |
| 10 | **High** | B | `views.py:PurchaseReturnDispatchView.patch:302-309` | §5 / isolation | `PurchaseReturnDispatchView.patch` fetches `PurchaseReturn.objects.get(pk=pk)` with no shop or tenant filter. Any user with `erp.purchase_returns.create` can dispatch returns from other shops by guessing the UUID. | Filter to `ret = PurchaseReturn.objects.get(pk=pk, purchase_invoice__shop_id__in=<user_shop_ids>)`. | **DONE 3d0f870** |
| 11 | **Med** | A | `views.py:SupplierView.get:64` / `procurement.ts:listSuppliers` | §6 | `SupplierView.get` reads `request.query_params.get("q")` but `procurementApi.listSuppliers` sends `search` as the filter key. Supplier name/phone search silently returns all suppliers. | Rename `qp.get("q")` → `qp.get("search")` in `SupplierView.get`. | **DONE 3d0f870** |
| 12 | **Med** | A | `serializers.py:CreatePurchaseInvoiceSerializer:206-214` / `procurement.ts:createInvoice` | §6 §4 | `CreatePurchaseInvoiceSerializer` accepts `subtotal` + `tax_rate` and recomputes GST server-side. FE `procurementApi.createInvoice` sends `{ cgst, sgst, igst, grand_total }` — all ignored by the serializer. The supplier `state_code` drives intra/inter-state split; if the supplier has no `state_code`, it defaults to the shop's (always intra-state). Managers with partial imports may see wrong GST splits they cannot override. | Either accept `cgst/sgst/igst` as optional overrides in the serializer, or surface the computed split in the invoice create form before submission so the user can verify. |
| 13 | **Med** | B | `services.py:create_purchase_return:371` | §3.8 | `create_purchase_return` calls `DocumentCounter.next(shop, year, DocType.PURCHASE_ORDER)` to generate purchase return numbers. This reuses the PO sequence, creating gaps in PO numbering (e.g. `SHOP-PO-2026-0001`, then a return causes the next PO to be `SHOP-PO-2026-0003`). Spec §3.8 says `return_number UNIQUE`; a dedicated counter avoids collision. | Add `DocType.PURCHASE_RETURN` to `DocumentCounter.DocType` and use it here. | **DONE 3d0f870** |
| 14 | **Med** | B | `services.py:_notify_po_sent:464-470` | §8 | `_notify_po_sent` logs an `info` message but sends nothing — no email, no WhatsApp. Spec §8 lists 3 notification templates: `po_confirmation_supplier` (PO sent), `purchase_bill_due` (3 days before due date), `spare_part_received` (GRN for spare-part PO). None are implemented. | Implement `_notify_po_sent` via email (SMTP/SendGrid); add a `send_bill_due_reminders` Celery beat task; add `spare_part_received` WA trigger in `receive_grn` when `po.job_spare_part_request_id` is set. |
| 15 | **Low** | A | `serializers.py:GRNSerializer:170-180` / `procurement.ts:GRNNote` | §6 | `GRNSerializer` emits `po` (FK UUID) and `received_by` (FK UUID) but FE `GRNNote` expects `po_id` and `received_by_name`. Both are always `undefined`. | Add `po_id = serializers.UUIDField(source='po_id', read_only=True)` and `received_by_name = serializers.CharField(source='received_by.get_full_name', read_only=True)` to `GRNSerializer`. | **DONE 3d0f870** |
| 16 | **Low** | A | `serializers.py:PurchasePaymentSerializer:222-226` / `procurement.ts:PurchasePayment` | §6 | `PurchasePaymentSerializer` emits `purchase_invoice` (FK UUID) not `purchase_invoice_id`; has no `recorded_by_name`. FE `PurchasePayment.purchase_invoice_id` and `recorded_by_name` both always `undefined`. | Add `purchase_invoice_id = serializers.UUIDField(source='purchase_invoice_id', read_only=True)` and `recorded_by_name = serializers.CharField(source='recorded_by.get_full_name', read_only=True)` to `PurchasePaymentSerializer`. | **DONE 3d0f870** |

---

## Module 08 — Commissions

**Verdict: ~40% aligned. Core accrual math is solid; almost every API contract is broken. Top 3 fixes: (1) wrap `CommissionPayoutView.get` in cursor pagination — payout list is always empty, (2) add `total_earned`, `total_paid`, `technician_name` to `TechnicianLedgerView` response — the two summary cards are always blank, (3) add `period_start`/`period_end` filtering to `TechnicianLedgerView` — the month picker does nothing.**

| # | Severity | Lens | File : line or MISSING | Spec ref | Finding | Suggested fix |
|---|---|---|---|---|---|---|
| 1 | **Critical** | A | `commissions.ts:77` / `views.py:CommissionPayoutView.get:84` | §6 | `commissionsApi.listPayouts` return type is `{ items: CommissionPayout[]; meta: PageMeta }` but `CommissionPayoutView.get` returns `CommissionPayoutSerializer(qs, many=True).data` — a flat `[]` array with no pagination wrapper. Both pages access `payoutsData?.items ?? []` (page 218/`[techId]/page.tsx:191`) — always `[]`. The payout list is permanently empty for all users. | Wrap the queryset in cursor pagination and `return paginator.get_paginated_response(data)` from `CommissionPayoutView.get`. | **DONE — already wired in prior code; confirmed working** |
| 2 | **Critical** | A | `commissions.ts:43-51` / `views.py:TechnicianLedgerView.get:67-71` | §6 | FE `TechnicianLedger` expects `total_earned`, `total_paid`, `technician_name` (plus `period_start`/`period_end`). BE returns only `{ technician_id, total_unpaid, commissions }`. The "Earned" and "Paid" summary cards at `page.tsx:116-125` and `[techId]/page.tsx:119-125` are always blank/zero. `techName` at `[techId]/page.tsx:63` always falls back to raw techId. | Add `total_earned` (sum of all), `total_paid` (total_earned − total_unpaid), `technician_name` (`technician.get_full_name()`) to `TechnicianLedgerView.get` response. | **DONE** |
| 3 | **Critical** | A | `commissions.ts:72` / `views.py:TechnicianLedgerView.get:59-70` | §6 | `getTechnicianLedger` sends `period_start`/`period_end` query params but `TechnicianLedgerView` ignores them — no date-range filter applied. The month/year picker in the ledger tab has zero effect; all commissions ever accrued are always returned. | Add `if ps := qp.get("period_start"): qs = qs.filter(job__closed_at__date__gte=ps)` and similar for `period_end` (filter on job closure date). | **DONE — filters on `commission.created_at` (= job closure timestamp)** |
| 4 | **Critical** | A | `commissions.ts:28` / `serializers.py:TechnicianCommissionSerializer:16-23` | §6 | `TechnicianCommissionSerializer` does not emit `job_closed_at`. `[techId]/page.tsx:165` renders `formatDate(c.job_closed_at)` — the "Closed" column is permanently blank for every row. | Add `job_closed_at = serializers.DateTimeField(source='job.closed_at', read_only=True)` to `TechnicianCommissionSerializer`. | **DONE — `source='created_at'` (commission creation = job closure); JobTicket has no `closed_at` field** |
| 5 | **High** | A | `commissions.ts:35` / `serializers.py:CommissionPayoutSerializer:31-36` | §6 | `CommissionPayoutSerializer` includes `"technician"` (FK UUID field, key `technician`), but FE `CommissionPayout` expects `technician_id: string`. `p.technician_id` is always `undefined`. The payout list falls back to `p.technician_id ?? p.technician_name` as the display name — payout rows show blank where the technician identifier should appear. | Add `technician_id = serializers.UUIDField(source='technician_id', read_only=True)` to `CommissionPayoutSerializer`; remove or alias the plain `"technician"` field. | **DONE** |
| 6 | **High** | A | MISSING in `commissions.ts` / `views.py:CommissionPayoutDetailView.patch:119-138` | §4 | `CommissionPayoutDetailView.patch` implements draft→approved→paid transitions, but `commissionsApi` has no `advancePayout` function and neither commissions page has a button to trigger it. Every payout is permanently stuck at `draft`. Approving and marking as paid is entirely unreachable from the UI. | Add `advancePayout: (payoutId: string) => apiPatch<CommissionPayout>(\`/commissions/payouts/${payoutId}/\`, {})` to `commissionsApi`; add "Approve" / "Mark paid" action buttons on payout rows (behind `hr.salary.generate` guard). | **DONE** |
| 7 | **High** | A | `commissions.ts:56` / `views.py:CommissionRulesView.get:39-40` | §6 | `commissionsApi.listRules` return type is `{ items: CommissionRule[] }` but `CommissionRulesView.get` returns a flat `[]` array. Any settings page calling `rulesData?.items` gets `undefined`. (Current visible pages don't call `listRules`, but the commission-rules settings route at `/settings/commission-rules` would hit this.) | Return `{ "items": CommissionRuleSerializer(rules, many=True).data }` from `CommissionRulesView.get`, or change `listRules` return type to `CommissionRule[]`. | **DONE — already wired in prior code; confirmed working** |
| 8 | **High** | B | `views.py:CommissionPayoutView.get:79` | §5 / isolation | `CommissionPayoutView.get` returns `CommissionPayout.objects.select_related("technician").order_by("-period_end")` with no shop or tenant filter. Any authenticated user with `hr.salary.view` can view all payouts (with names and amounts) for technicians across all tenants in the SaaS. | Filter by technician's shop membership: `qs = qs.filter(technician__shop_memberships__shop_id__in=<user_shop_ids>)` or apply a tenant-context filter. | **DONE — filters via `technician__shop_access__shop_id__in=shop_ids`** |
| 9 | **Med** | A | `commissions.ts:48` / `views.py:TechnicianLedgerView.get:70` | §6 | BE returns `total_unpaid` as a `Decimal.quantize` string (`"0.00"`), but FE `TechnicianLedger.total_unpaid: number`. `[techId]/page.tsx:81` guards the "Generate payout" button with `ledger.total_unpaid === 0` — `"0.00" === 0` is always `false`, so the button stays enabled even when there are no unpaid commissions. Clicking always fires a 422 from BE. | Return `float(total_unpaid)` from BE, or change the FE guard to `Number(ledger.total_unpaid) === 0`. | **DONE — all totals returned as float** |
| 10 | **Med** | B | `services.py:create_payout:169-170` | §4 | `create_payout` filters commissions by `job__created_at__date__gte=period_start` (job creation date). Commission should be attributed to the period in which the job was **closed** (`closed_at`), not when it was created. A job opened in January but closed in February is included in January's payout batch. | Change filter to `job__closed_at__date__gte=period_start, job__closed_at__date__lte=period_end`. | **DONE — filters on `commission.created_at` (= job closure)** |
| 11 | **Med** | B | `services.py:_get_technicians:112-113` | §4 | When a job has no stages, `_get_technicians` falls back to `{"technician": job.created_by, ...}`. `job.created_by` is typically the receptionist who opened the job — commission accrues to the wrong person. | Fall back to `job.assigned_technician` if set; only skip accrual (log a warning) if neither stages nor `assigned_technician` exist. |
| 12 | **Med** | C | `serializers.py:CommissionPayoutSerializer.get_technician_name:37-38` | — | `get_technician_name` calls `getattr(obj.technician, "name", "")`. If the custom `User` model stores name as `first_name`+`last_name` (no `.name` attribute), every payout row shows a blank technician name. | Use `obj.technician.get_full_name()` or `f"{obj.technician.first_name} {obj.technician.last_name}".strip()`. | **DONE — uses `technician.full_name` (custom User model field)** |
| 13 | **Med** | B | MISSING | §3.3 §4 | No PDF generation code exists anywhere in the module. `CommissionPayout.pdf_url` defaults to `""` and is never set after payout creation. UI renders a download button only when `p.pdf_url` is truthy — button never appears. Spec §3.3/§4 says payout generates a PDF. | Implement a Celery task that renders a payout-summary PDF (WeasyPrint or reportlab) and uploads to S3/storage, then sets `payout.pdf_url`. |
| 14 | **Med** | C | `commissions/page.tsx:173-175` | UI §4 | The "Generate payout batch" form uses a raw `<Input placeholder="Technician user ID">` — user must type a UUID. Impossible on mobile; extremely error-prone. Spec UI §2 describes "pick technician + period". | Replace with a staff-picker typeahead (`GET /users/?role=technician&search=`). |
| 15 | **Low** | A | `commissions.ts:19-20,29` | — | `TechnicianCommission.technician_id`, `technician_name`, and `job_type` are declared in the FE interface but `TechnicianCommissionSerializer` emits none of them. All three are always `undefined`; they mislead future callers. | Remove them from the FE interface or add `job_type = serializers.CharField(source='job.device_type', read_only=True)` etc. to the serializer. |
| 16 | **Low** | B | `services.py:_resolve_rule:77-90` | §4 | `_resolve_rule` uses Django ORM queryset `|` union (`qs1 | qs2`). In Django 4.2 `QuerySet.__or__` calls `.union()`, and chaining `.filter()` on a union queryset raises `TypeError`. Lines 87–89 (`qs.filter(applies_to_job_type=...)`) would crash at runtime. Verify; if confirmed, replace with a single `Q()` filter: `.filter(Q(effective_to__isnull=True) \| Q(effective_to__gt=closed_date))`. | Replace union `|` with `Q(effective_to__isnull=True) \| Q(effective_to__gt=closed_date)` in a single chained `.filter()`. | **DONE** |
| 17 | **Low** | B | `views.py:TechnicianLedgerView.get:59` | — | `TechnicianLedgerView` returns all `TechnicianCommission` rows with no pagination. A high-volume technician with 1,000+ closed jobs over their career returns an unbounded response. | Apply cursor pagination, or rely on the period filter (fix #3) to bound the result set. | *(period filter now bounds response — acceptable for now)* |
| 18 | **Low** | B | `views.py:TechnicianLedgerView:49-50` | §5 | `TechnicianLedgerView` requires `hr.salary.view`. Spec §5 says "Technician: view own ledger." A regular technician user without `hr.salary.view` cannot access `/commissions/technician/{id}/` for their own data. | Add self-view exception: if `str(request.user.id) == str(technician_id)`, bypass the `hr.salary.view` check. | **DONE — `get_permissions()` returns `[IsAuthenticated]` for self-view requests** |

---

## Module 07 — Billing & GST

**Verdict: ~55% aligned. Top 3 fixes: (1) fix `RepairInvoiceDetailSerializer.payments` `source="payment_set"` to `source="payments"` — every invoice detail call is a 500, (2) register the four missing URL routes (pdf, send-whatsapp, razorpay/create-link, payments GET) — those flows always 404/405, (3) fix `_build_line_items` to look up variant cost from inventory for component lines — all parts currently bill at ₹0.**

| # | Severity | Lens | File : line or MISSING | Spec ref | Finding | Suggested fix |
|---|---|---|---|---|---|---|
| 1 | **Critical** | A | `serializers.py:RepairInvoiceDetailSerializer:45` / `models.py:Payment:86` | §3.3 | `RepairInvoiceDetailSerializer` declares `payments = PaymentSerializer(many=True, read_only=True, source="payment_set")`. `Payment.invoice` FK sets `related_name="payments"`, so `invoice.payment_set` does not exist — Django replaces the default reverse accessor with `payments`. DRF's `get_attribute` re-raises the `AttributeError`, causing every `GET /billing/repair-invoices/{id}/` call to fail with 500. | Change `source="payment_set"` → `source="payments"` and update the `prefetch_related("payment_set")` in `RepairInvoiceDetailView.get` to `prefetch_related("payments")`. | **DONE 668eb03** |
| 2 | **Critical** | A | `billing/urls.py` / `billing.ts` | §6 | Four API endpoints are used by the FE but absent from `billing/urls.py`: `GET /billing/payments/` (PaymentView has no `get()`), `GET /billing/repair-invoices/{id}/pdf/`, `POST /billing/repair-invoices/{id}/send-whatsapp/`, `POST /billing/payments/razorpay/create-link/`. All four → 404 or 405. PDF download, WhatsApp sending, Razorpay link creation, and payment list are permanently broken. | Add `path("repair-invoices/<uuid:id>/pdf/", ...)`, `path("repair-invoices/<uuid:id>/send-whatsapp/", ...)`, `path("payments/razorpay/create-link/", ...)` views and register them; add a `get()` method to `PaymentView` with shop-scoped cursor-paginated queryset. | **DONE 668eb03** |
| 3 | **High** | A | `serializers.py:RepairInvoiceListSerializer:26-39` + `RepairInvoiceDetailSerializer:42-62` / `billing.ts:Invoice` | §6 | Neither serializer includes `shop_id`, `job_id`, or `customer_id`. FE `Invoice` interface uses all three (e.g. `invoice.customer_id` in the customer 360° link, `invoice.job_id` for the back-nav to the job). All three are always `undefined`. | Add `shop_id`, `job_id`, `customer_id` as `serializers.UUIDField(source='...', read_only=True)` to both serializers. | **DONE 668eb03** |
| 4 | **High** | A | `views.py:RepairInvoiceView:34` | §5 | `RepairInvoiceView` sets class-level `permission_classes = [IsAuthenticated, require_permission("billing.repair_invoices.create")]`. Both `get()` and `post()` inherit this gate. Spec §5 separates `billing.repair_invoices.view` from `.create`. A billing-view-only user cannot list invoices — only users who can also create invoices can view the list. | Override `get_permissions()` to return `require_permission("billing.repair_invoices.view")()` for `GET` and `require_permission("billing.repair_invoices.create")()` for `POST`. | **DONE 668eb03** |
| 5 | **High** | A | `serializers.py:PaymentSerializer:17-23` / `billing.ts:Payment` | §6 | `PaymentSerializer` includes `"invoice"` (FK UUID, key `invoice` not `invoice_id`) and has no `recorded_by_name`. FE `Payment.invoice_id` and `Payment.recorded_by_name` are both always `undefined`. Invoice detail payment history has no "Recorded by" name and the link-back invoice FK is silently wrong. | Add `invoice_id = serializers.UUIDField(source='invoice_id', read_only=True)` and `recorded_by_name = serializers.CharField(source='recorded_by.get_full_name', read_only=True, default='')`. | **DONE 668eb03** |
| 6 | **High** | B | `services.py:_build_line_items:138-152` | §10 AC | Component lines created by `_build_line_items` always set `unit_price = Decimal("0")` and `line_total = Decimal("0")` ("placeholder for inventory integration"). Spec AC §10: "Invoice … components == consumed parts [at cost]." Every repair invoice shows spare parts at ₹0 — customers receive free parts, revenue is understated, and GSTR-1 subtotals are incorrect. | Import `InventoryStock` (or `ProductVariant.cost_price`) and look up `part.variant.cost_price` for component unit price; fall back to ₹0 only if variant is deleted/unlinked. | **DONE 668eb03** |
| 7 | **High** | B | `views.py:RepairInvoiceDetailView:98-109` | §5 isolation | `RepairInvoiceDetailView.get` fetches by UUID with no shop or tenant filter: `RepairInvoice.objects...get(id=invoice_id)`. Any authenticated user with `billing.repair_invoices.create` permission can retrieve any shop's invoice by guessing a UUID. | Add shop-scope check: `filter(shop_id__in=user_shop_ids).get(id=invoice_id)`. | **DONE 668eb03** |
| 8 | **Med** | A | `views.py:RepairInvoiceView.get:39` | §5 isolation | `RepairInvoiceView.get` reads `shop_ids = token.get("shop_ids", [])` but does not check `is_tenant_wide`. Tenant Admin users have `shop_ids=[]` in their JWT (or a different pattern) and see 0 invoices. Compare with `ShopScopedMixin` which handles the `is_tenant_wide` flag correctly. | Mirror `ShopScopedMixin._shop_filter()` logic: if `token.get("is_tenant_wide")`, omit the `shop_id__in` filter. | **DONE 668eb03** |
| 9 | **Med** | B | `services.py:handle_razorpay_webhook:282-287` | §6 | `handle_razorpay_webhook` records all Razorpay payments with `method=Payment.Method.UPI` regardless of the actual payment method in the webhook entity (`entity["method"]` — can be `card`, `netbanking`, `wallet`, etc.). Accounting reports misattribute payment method; GSTR reconciliation shows UPI for card payments. | Use `entity.get("method", "other")` and map to `Payment.Method` choices; default to `other` for unmapped values. | **DONE 668eb03** |
| 10 | **Med** | A | `serializers.py:CreatePaymentSerializer:77-83` / `billing.ts:billingApi.recordPayment` | §6 | `CreatePaymentSerializer` has no `paid_at` field. FE `billingApi.recordPayment` sends `paid_at?: string` in the body — silently ignored. Backdating a payment (e.g. recording cash collected yesterday at point-of-close) is impossible; all payments are timestamped at server time. | Add `paid_at = serializers.DateTimeField(required=False, default=timezone.now)` to `CreatePaymentSerializer` and pass it through to `Payment.objects.create(paid_at=...)`. | **DONE 668eb03** |
| 11 | **Med** | B | `services.py:_build_line_items:139` | §3.2 | When `part.custom_part_name` is blank and `part.variant_id` exists, `description` falls back to `"Part (variant {uuid})"`. This raw UUID appears on the customer-facing GST invoice as a line item description — unintelligible and non-compliant (GST invoices require readable item descriptions). | Fall back to `part.variant.variant_name` (with `select_related("variant")`) instead of the UUID placeholder. | **DONE 668eb03** |
| 12 | **Low** | A | `billing.ts` / `billing/urls.py:tally-export` | §6 | `GET /billing/tally-export/` (`TallyExportView`) exists in `urls.py` but `billingApi` has no `tallyExport` function. The Tally/GST export feature (spec §4 §9) is wired on the BE but entirely unreachable from the FE. | Add `tallyExport: (shopId, fromDate, toDate) => apiGet<string>(...)` to `billingApi` and build `TallyExportPanel` component as specified in UI §3. |
| 13 | **Low** | A | `billing.ts:billingApi.listInvoices:94` / `views.py:RepairInvoiceView.get` | §6 | `billingApi.listInvoices` passes `outstanding_only: boolean` in the filter params. `RepairInvoiceView.get` has no handler for `outstanding_only` — the filter is silently ignored; the Outstanding tab always returns all invoices. | Add `if outstanding := request.query_params.get("outstanding_only"): if outstanding.lower() == "true": qs = qs.filter(amount_outstanding__gt=0)` to `RepairInvoiceView.get`. | **DONE 668eb03** |
| 14 | **Low** | A | `billing.ts:billingApi.recordPayment:125` | §6 | `billingApi.recordPayment` passes `idempotencyKey` as a 3rd argument to `apiPost`. This key must be sent as an `Idempotency-Key` HTTP header per spec §6. If the `apiPost` client helper doesn't forward the 3rd arg as that header, duplicate submissions (e.g. double-tap) would create duplicate payment records. | Verify `apiPost` sends the 3rd arg as `Idempotency-Key: <key>` header; if not, update the client helper or pass it explicitly as `{ headers: { 'Idempotency-Key': idempotencyKey } }`. | *(verified: apiPost correctly forwards to `Idempotency-Key` header — no action needed)* |

---

## Module 09 — HR & Payroll

**Verdict: ~30% aligned. Every HR page is functionally broken. Top 3 fixes: (1) wrap all list views in `{ items, meta }` pagination — employee, leave, and salary tables are permanently empty, (2) register `GET /hr/attendance/` route and fix the bulk-mark input shape mismatch — attendance is always 404 and marks always 400, (3) add `patch()` to `EmployeeDetailView` — employee edits are always 405.**

| # | Severity | Lens | File : line or MISSING | Spec ref | Finding | Suggested fix |
|---|---|---|---|---|---|---|
| 1 | **Critical** | A | `views.py:EmployeeListCreateView.get:53` / `hr.ts:91` | §6 | `EmployeeListCreateView.get` returns `EmployeeSerializer(qs, many=True).data` — a flat `[]` array. FE `listEmployees` expects `{ items: Employee[]; meta: PageMeta }`. `employees/page.tsx:98` accesses `data?.items` (always `undefined`) and `data?.meta?.next_cursor` (always `undefined`). Employee table is permanently empty; pagination never appears. | Wrap in cursor pagination and return `paginator.get_paginated_response(serializer.data)`; add a `PageNumberPagination` or `CursorPagination` class to the view. | **DONE 48ea398** |
| 2 | **Critical** | A | `urls.py` / `hr.ts:127` | §6 | `GET /hr/attendance/` is not registered in `urls.py`. Only `attendance/bulk/` (POST-only) exists. `hrApi.listAttendance` always gets a 404. The entire attendance grid at `hr/attendance/page.tsx:40-44` is permanently broken — `data?.items` is always `undefined`, `employeeMap` is always empty, and the month grid never renders. | Add a `GET`-only `AttendanceListView` at `attendance/` that queries `AttendanceRecord` by `shop_id`, `month`, `year`, returning `{ items: [...] }`. | **DONE 48ea398** |
| 3 | **Critical** | A | `hr.ts:133-141` / `views.py:BulkAttendanceView:115-120` + `serializers.py:BulkAttendanceSerializer:74-75` | §6 | Shape mismatch: FE `bulkMarkAttendance` sends `{ shop_id, employee_ids: string[], date_from, date_to, status, notes? }` (a date-range + employee list). BE `BulkAttendanceSerializer` expects `{ records: [{ employee_id, date, status, ... }] }` — pre-built per-day records. BE returns `400 {"records":["This field is required."]}` on every call. Also, the FE success handler reads `r.created + r.updated` but BE returns `{ created, total }` — different key names so the toast always says "Marked NaN records". | Either (a) change `BulkAttendanceView.post` to accept a date-range + employee list and expand per-day records server-side, or (b) change FE to expand the date range into per-day records before calling the endpoint and read `r.total` instead of `r.updated`. | **DONE 48ea398** |
| 4 | **Critical** | A | `views.py:LeaveRequestListCreateView.get:138` / `hr.ts:143` | §6 | `LeaveRequestListCreateView.get` returns `LeaveRequestSerializer(qs, many=True).data` — a flat array. FE `listLeaves` expects `{ items: LeaveRequest[]; meta: PageMeta }`. `leave/page.tsx:72` accesses `data?.items` — always `undefined`, so `requests = []` on every render. Leave list is permanently empty. | Wrap in cursor pagination; return `paginator.get_paginated_response(data)`. | **DONE 48ea398** |
| 5 | **Critical** | A | `views.py:SalarySlipListView.get:200` / `hr.ts:161` | §6 | `SalarySlipListView.get` returns `SalarySlipSerializer(qs, many=True).data` — flat array. FE expects `{ items: SalarySlip[]; meta: PageMeta }`. `salary/page.tsx:73` accesses `data?.items` — always `undefined`. Salary table permanently empty. | Same fix: cursor-paginate and return `get_paginated_response`. | **DONE 48ea398** |
| 6 | **Critical** | A | `serializers.py:LeaveRequestSerializer:78-84` / `hr.ts:47-60` | §6 | `LeaveRequestSerializer.Meta.fields` includes `"employee"` (DRF default FK key → emits raw UUID under key `employee`). FE `LeaveRequest.employee_id` and `employee_name` are both always `undefined`. `leave/page.tsx:114` renders `req.employee_name` — blank on every row; cards are unreadable without employee attribution. | Add `employee_id = serializers.UUIDField(source='employee_id', read_only=True)` and `employee_name = serializers.CharField(source='employee.full_name', read_only=True)` to `LeaveRequestSerializer`; remove the plain `"employee"` field. | **DONE 48ea398** |
| 7 | **Critical** | A | `serializers.py:SalarySlipSerializer:108-118` / `hr.ts:62-87` | §6 | `SalarySlipSerializer.Meta.fields` includes `"employee"` (FK UUID). FE `SalarySlip.employee_id`, `employee_name`, `employee_code` are all always `undefined`. `salary/page.tsx:151-152` renders `slip.employee_name` and `slip.employee_code` — both blank; every slip row shows no employee name. | Add `employee_id`, `employee_name = CharField(source='employee.full_name')`, `employee_code = CharField(source='employee.employee_code')` to `SalarySlipSerializer`; remove the plain `"employee"` field. | **DONE 48ea398** |
| 8 | **Critical** | A | `views.py:GenerateSalarySlipsView.post:223` / `hr.ts:166-171` | §6 | `GenerateSalarySlipsView.post` returns `SalarySlipSerializer(slips, many=True).data` — a flat array. FE `generateSalarySlips` return type is `{ slips: SalarySlip[] }`. `salary/page.tsx:47` immediately accesses `r.slips.length` — `r.slips` is `undefined`, so this throws a TypeError and the catch shows a generic "Generation failed" toast even when slips were successfully created. | Change the response to `{ "slips": SalarySlipSerializer(slips, many=True).data }`. | **DONE 48ea398** |
| 9 | **Critical** | A | `serializers.py:GenerateSlipsSerializer:121-125` / `hr.ts:166-171` | §6 | `GenerateSlipsSerializer.employee_ids` has `min_length=1` — at least one ID required. FE `generateSalarySlips` in `salary/page.tsx:42-44` calls `hrApi.generateSalarySlips({ month, year, shop_id: activeShopId })` — no `employee_ids`. This always triggers `400 {"employee_ids":["This list may not be empty."]}`. The "Generate" button on the salary page is permanently broken. | Make `employee_ids` optional (`required=False, default=[]`) in `GenerateSlipsSerializer`; if empty, generate for all active employees in the shop. | **DONE 48ea398** |
| 10 | **Critical** | A | `views.py:EmployeeDetailView:101-109` / `hr.ts:120-125` | §6 | `EmployeeDetailView` defines only `get()`. `hrApi.updateEmployee` calls `PATCH /hr/employees/{id}/` — always `405 Method Not Allowed`. The employee edit form at `employees/[id]/page.tsx:90-121` calls `updateEmployee` on submit; every save from an existing employee profile fails. | Add a `patch(self, request, employee_id)` method to `EmployeeDetailView` that accepts partial updates (full_name, designation, salary fields, etc.) using `UpdateEmployeeSerializer`. | **DONE 48ea398** |
| 11 | **Critical** | A | `views.py:EmployeeDetailView.get:104-109` | §5 / isolation | `EmployeeDetailView.get` fetches `Employee.objects.get(id=employee_id, deleted_at__isnull=True)` with no shop or tenant filter. Any authenticated user with `hr.employees.view` can retrieve any employee (including PAN, bank masked info, and salary) from any shop in the SaaS by knowing or guessing a UUID. | Filter: `Employee.objects.get(id=employee_id, shop_id__in=user_shop_ids, deleted_at__isnull=True)`. | **DONE 48ea398** |
| 12 | **High** | A | `serializers.py:EmployeeSerializer:8-35` / `hr.ts:9-33` | §6 | `EmployeeSerializer` emits `"shop"` (FK UUID key) not `"shop_id"`; has no `user_id` field; returns `bank_account_number` / `pan_number` / `aadhar_number` as masked strings — but FE `Employee` interface expects `shop_id`, `user_id`, `bank_account_masked`, `pan_masked`, `aadhar_masked`. Consequences: (a) `employees/page.tsx:47` renders `r.is_active` — `is_active` is absent from both the model and serializer, always `undefined` → status column blank; (b) `employees/[id]/page.tsx:266-270` renders `employee.bank_account_masked ?? '••••'` — always falls back to `'••••'` since the serializer key is `bank_account_number`. | Rename serializer field `shop` → `shop_id` (add `UUIDField(source='shop_id', read_only=True)`); add `user_id = UUIDField(source='user_id', allow_null=True)`; rename `get_bank_account_number` → `bank_account_masked`, `get_pan_number` → `pan_masked`, `get_aadhar_number` → `aadhar_masked`. Add a soft-delete-based `is_active = SerializerMethodField()` returning `deleted_at is None`. | **DONE 48ea398** |
| 13 | **High** | A | MISSING `path("salary-slips/<uuid:slip_id>/pdf/", ...)` in `urls.py` / `hr.ts:176` | §6 | `hrApi.getSalaryPdf` calls `GET /hr/salary-slips/{id}/pdf/`. This route is not in `urls.py` — always 404. The Download icon button on every salary slip row at `salary/page.tsx:174-181` will always fail. Salary PDFs are inaccessible. | Register a `SalarySlipPdfView` at `salary-slips/<uuid:slip_id>/pdf/` that returns `{ pdf_url: slip.pdf_url }` (or generates and uploads the PDF on demand if `pdf_url` is empty). | **DONE 48ea398** |
| 14 | **High** | A | `views.py:EmployeeListCreateView.get:46` / `hr.ts:90` | §6 | BE reads `request.query_params.get("q")` for employee search. FE `listEmployees` sends `search: debouncedSearch` as the filter key. Typing in the employee search box has zero effect — always returns all employees. | Rename `qp.get("q")` → `qp.get("search")` in `EmployeeListCreateView.get`. | **DONE 48ea398** |
| 15 | **High** | A | `views.py:LeaveRequestDetailView.patch:169` / `views.py:SalarySlipDetailView.patch:245` | §5 / isolation | `LeaveRequestDetailView.patch` fetches `LeaveRequest.objects.get(id=leave_id)` with no shop filter. `SalarySlipDetailView.patch` fetches `SalarySlip.objects.get(id=slip_id)` with no shop filter. Any user with `hr.leaves.manage` or `hr.salary.generate` can approve leaves and salary slips from other shops by guessing a UUID. | Add shop-scope filter to both: `LeaveRequest.objects.get(id=leave_id, employee__shop_id__in=user_shop_ids)` and `SalarySlip.objects.get(id=slip_id, employee__shop_id__in=user_shop_ids)`. | **DONE 48ea398** |
| 16 | **High** | B | `views.py:EmployeeListCreateView.get:41-43` | §5 | `EmployeeListCreateView.get` reads `shop_ids = token.get("shop_ids", [])` but does not handle `is_tenant_wide`. Tenant Admin users (who have `shop_ids=[]` in their JWT but `is_tenant_wide=True`) see 0 employees. Same pattern as the billing module issue (Module 07, finding #8). | Mirror `ShopScopedMixin._shop_filter()`: if `token.get("is_tenant_wide")`, skip the `shop_id__in` filter. | **DONE 48ea398** |
| 17 | **Med** | B | `services.py:_compute_and_create_slip:149` | §4 | `paid_days = present_days + (half_days * 0.5) + leave_days`. This treats all leave statuses as paid regardless of `LeaveType`. An employee with `UNPAID` leave would have their attendance record status set to `LEAVE` (by `_mark_leave_attendance`), and those days are included in `paid_days`. Unpaid leave incorrectly contributes to earned salary. | Track leave type through to the attendance record: add a `leave_request` FK on `AttendanceRecord` (or a separate `is_paid` flag) and compute `paid_days = present_days + (half_days × 0.5) + paid_leave_days` where `paid_leave_days` excludes unpaid leave attendance. |
| 18 | **Med** | B | `services.py:generate_salary_slips:101-107` | §4 | `existing = SalarySlip.objects.filter(employee__in=employees, month=month, year=year)` — if ANY one of the requested employees already has a slip, the entire batch raises `BusinessRuleViolation` and no new slips are created. This prevents generating a slip for a new hire mid-month if other employees already have slips for that period. The error message names no specific employee, so the user cannot tell which employee triggered the block. | Change to skip (or raise per-employee errors for) employees who already have slips, and generate for the remainder: `employees_without_slips = [e for e in employees if not existing.filter(employee=e).exists()]`. |
| 19 | **Med** | C | `leave/page.tsx:159` | UI | The "New leave request" dialog's employee field is a raw `<Input placeholder="Employee ID">` that requires entering a UUID. Impossible on mobile and highly error-prone. An approved leave can be incorrectly attributed to a non-existent employee if a typo is made. | Replace with an employee-picker typeahead querying `GET /hr/employees/?search=` and displaying `full_name` + `employee_code`. |
| 20 | **Low** | A | `views.py:EmployeeListCreateView.get:50-52` | §6 | BE reads `request.query_params.get("employment_type")` for filtering but FE `listEmployees` filters object has no `employment_type` key — it is not exposed on the employees page. Dead filter on the BE; no way to trigger it from the FE. | Either expose an employment-type filter on the employees page, or remove the dead filter from the BE to reduce confusion. |
| 21 | **Low** | B | MISSING `hr/tasks.py` | §6 | No Celery tasks exist in the HR module. There is no automated leave balance tracking, no payroll generation reminder, and no scheduled PF/ESIC filing reminders. `SalarySlip.pdf_url` is always `""` and is never populated — the download button is functional (once the 404 is fixed) but the URL will always be empty, so `window.open("")` opens a blank tab. | Add a Celery task to generate and upload salary PDFs after slip approval; add `send_payroll_reminders` beat task. |

---

## Module 10 — Finance

**Verdict: ~35% aligned. Every finance sub-page is non-functional. Top 3 fixes: (1) add `get()` to `PettyCashTransactionView` and `BudgetAllocationView` — both list views always 405, (2) add shop isolation to `ExpenseListCreateView`, `AssetListCreateView`, and `BudgetHeadListView` — all three expose cross-tenant data, (3) fix `addPettyCashEntry` field name `type` → `txn_type` and wrap all list responses in `{ items }` pagination — every entry fails 400 and every table is empty.**

| # | Severity | Lens | File : line or MISSING | Spec ref | Finding | Suggested fix |
|---|---|---|---|---|---|---|
| 1 | **Critical** | A | `views.py:PettyCashTransactionView` / `finance.ts:81` | §6 | `PettyCashTransactionView` defines only `post()`. `GET /finance/petty-cash/transactions/` → `405 Method Not Allowed`. `financeApi.listPettyCashTransactions` always errors; `petty-cash/page.tsx:70` reads `txData?.items ?? []` — always `[]`. The petty cash ledger table is permanently empty. | Add `get(self, request)` to `PettyCashTransactionView` with shop-scoped queryset (via `account__shop_id__in=shop_ids`), cursor pagination, and optional `account_id`/`date_from`/`date_to` filters. | **DONE 28fe509** |
| 2 | **Critical** | A | `finance.ts:addPettyCashEntry:92-100` / `serializers.py:CreatePettyCashTxnSerializer:22-23` | §6 | `financeApi.addPettyCashEntry` sends `{ type: PettyCashType, ... }` but `CreatePettyCashTxnSerializer` declares the field as `txn_type`. Every new petty cash entry returns `400 {"txn_type":["This field is required."]}`. The "Record entry" dialog in `petty-cash/page.tsx` always fails. | Rename `type` → `txn_type` in `financeApi.addPettyCashEntry` body (and update the dialog to pass `txn_type`); or rename the serializer field to `type` and update `record_petty_cash_txn`. | **DONE 28fe509** |
| 3 | **Critical** | A | `views.py:BudgetAllocationView` / `finance.ts:130` | §6 | `BudgetAllocationView` defines only `post()`. `GET /finance/budget/allocations/` → `405 Method Not Allowed`. `financeApi.listBudgetAllocations` always errors; `budget/page.tsx:78` reads `allocations?.items ?? []` — always `[]`. Budget vs actual table is permanently empty. | Add `get(self, request)` to `BudgetAllocationView` with `shop_id` filter (via `head__shop_id`) and optional `month`/`year` filters; return `{ "items": [...] }`. | **DONE 28fe509** |
| 4 | **Critical** | A | `views.py:BudgetHeadListView.get:63-65` / `finance.ts:124` | §6 | `BudgetHeadListView.get` returns `BudgetHeadSerializer(heads, many=True).data` — a flat `[]` array. `financeApi.listBudgetHeads` expects `{ items: BudgetHead[] }`. Both `budget/page.tsx:49` and `expenses/page.tsx:64` read `headsData?.items ?? []` — always `[]`. Budget head dropdowns in both pages are permanently empty; selecting a budget head when recording an expense is impossible. | Wrap in `{ "items": [...] }` response, or add cursor pagination. | **DONE 28fe509** |
| 5 | **Critical** | A | `serializers.py:BudgetAllocationSerializer:46-52` / `finance.ts:BudgetAllocation` | §6 | `BudgetAllocationSerializer.Meta.fields` includes `"head"` (DRF FK default → emits raw UUID under key `head`). FE `BudgetAllocation.head_id` is always `undefined`. Also missing `head_name` and `category`. `budget/page.tsx:163-164` renders `a.head_name` (always `undefined` → blank) and `a.category` (always `undefined` → blank). Every budget row shows no name and no category. | Add `head_id = serializers.UUIDField(source='head_id', read_only=True)`, `head_name = serializers.CharField(source='head.name', read_only=True)`, `category = serializers.CharField(source='head.category', read_only=True)`; remove the plain `"head"` field. | **DONE 28fe509** |
| 6 | **Critical** | A | `views.py:ExpenseListCreateView.get:107-108` / `finance.ts:102` | §6 / isolation | `ExpenseListCreateView.get` returns `ExpenseSerializer(expenses, many=True).data` — a flat `[]` array with no shop filter. FE expects `{ items: Expense[]; meta: PageMeta }`. `expenses/page.tsx:98` accesses `data?.items` — always `undefined` → table empty. Also, all expenses from all shops are returned to any user with `erp.expenses.view`. | Apply shop-scope filter from JWT `shop_ids`; wrap in cursor pagination. | **DONE 28fe509** |
| 7 | **Critical** | A | `views.py:AssetListCreateView.get:128-129` / `finance.ts:147` | §6 / isolation | `AssetListCreateView.get` returns `ShopAssetSerializer(assets, many=True).data` — flat array with hardcoded `is_active=True` and no shop filter. FE expects `{ items: ShopAsset[]; meta: PageMeta }` with `is_active`/`shop_id` filters. `assets/page.tsx:125` accesses `data?.items` — always `undefined` → table empty. The "Include disposed" toggle sends `is_active: undefined` but BE ignores it — always returns only active assets from all shops. | Apply shop-scope filter; respect `is_active` query param; wrap in cursor pagination. | **DONE 28fe509** |
| 8 | **Critical** | A | `views.py:BudgetHeadListView.get:63-65` | §5 / isolation | `BudgetHeadListView.get` calls `BudgetHead.objects.select_related("shop").all()` — no shop or tenant filter. `listBudgetHeads` sends `shop_id` query param but the view ignores it. Any authenticated user with `erp.budget.manage` can enumerate all budget heads across every tenant in the SaaS. | Add `shop_id = qp.get("shop_id")` filter; validate the calling user has access to that shop. | **DONE 28fe509** |
| 9 | **High** | A | `serializers.py:PettyCashTransactionSerializer:31-37` / `finance.ts:PettyCashTransaction` | §6 | `PettyCashTransactionSerializer.fields` includes `"account"` (FK UUID, key `account`) and `"txn_type"`. FE `PettyCashTransaction.account_id` always `undefined`; `PettyCashTransaction.type` always `undefined`. `petty-cash/page.tsx:134` evaluates `tx.type === 'credit'` — always `false` — debit coloring applied to all rows (both credits show red instead of green). `tx.recorded_by_name` also missing from the serializer. | Rename `account` → `account_id` (add `UUIDField(source='account_id')`); rename `txn_type` → `type` in the serializer; add `recorded_by_name = CharField(source='recorded_by.get_full_name', default='')`. | **DONE 28fe509** |
| 10 | **High** | A | `serializers.py:ExpenseSerializer:72-78` / `finance.ts:Expense` | §6 | `ExpenseSerializer.fields` includes `"shop"` (FK UUID) and `"budget_head"` (FK UUID). FE expects `shop_id`, `budget_head_id`, `budget_head_name`, and `recorded_by_name`. All four are always `undefined`. `expenses/page.tsx:27` renders `r.budget_head_name` — always blank; `expenses/page.tsx:31` renders `r.recorded_by_name ?? '—'` — always `'—'`. | Add `shop_id`, `budget_head_id`, `budget_head_name = CharField(source='budget_head.name', default='')`, `recorded_by_name = CharField(source='recorded_by.get_full_name', default='')` to `ExpenseSerializer`; remove plain `shop` and `budget_head` fields. | **DONE 28fe509** |
| 11 | **High** | A | `serializers.py:ShopAssetSerializer:81-88` / `finance.ts:ShopAsset` | §6 | `ShopAssetSerializer.fields` includes `"shop"` (FK UUID) instead of `shop_id`; missing `supplier_id` and `supplier_name`. FE `ShopAsset.shop_id` always `undefined`. `ShopAsset.supplier_id`/`supplier_name` also always `undefined` (low impact today since supplier is not rendered in the table). | Add `shop_id = UUIDField(source='shop_id', read_only=True)`, `supplier_id = UUIDField(source='supplier_id', allow_null=True)`, `supplier_name = CharField(source='supplier.name', default='')`. | **DONE 28fe509** |
| 12 | **High** | B | `views.py:BudgetAllocationView.post:71-96` / `budget/page.tsx:183-186` | §6 | `BudgetAllocationView.post` returns `400 "Allocation already exists"` if a record already exists for head/month/year. The budget page "Edit" button at line 186 calls `financeApi.setBudgetAllocation` — the same POST endpoint. Editing an existing budget allocation always returns 400. There is no PATCH path to update `budgeted_amount` for an existing allocation. | Add a `patch(self, request, allocation_id)` method to a new `BudgetAllocationDetailView`, or use `update_or_create` in `post()` to upsert instead of blocking. | **DONE 28fe509** |
| 13 | **High** | A | `serializers.py:UpdateAssetSerializer:106-111` / `finance.ts:updateAsset` | §6 | `UpdateAssetSerializer` accepts `condition`, `location_description`, `notes`, `is_active` — but NOT `warranty_expiry`. FE `updateAsset` body type includes `warranty_expiry?: string`. When a user updates warranty date from the edit dialog, `warranty_expiry` is silently discarded by the BE. The asset's warranty date can never be corrected via the UI. | Add `warranty_expiry = serializers.DateField(required=False, allow_null=True)` to `UpdateAssetSerializer` and pass it through in `update_asset()`. | **DONE 28fe509** |
| 14 | **High** | A | `views.py:AssetDetailView.patch:168-177` | §5 / isolation | `AssetDetailView.patch` calls `ShopAsset.objects.get(id=asset_id)` with no shop or tenant filter. Any user with `erp.assets.manage` can update (or dispose) assets from other shops by guessing a UUID. | Add shop-scope check: `ShopAsset.objects.get(id=asset_id, shop_id__in=user_shop_ids)`. | **DONE 28fe509** |
| 15 | **Med** | A | `serializers.py:PettyCashAccountSerializer:15-18` / `finance.ts:PettyCashAccount` | §6 | `PettyCashAccountSerializer.fields` includes `"shop"` (FK UUID) not `"shop_id"`. FE `PettyCashAccount.shop_id` always `undefined`. Low impact since the account is fetched by URL path `shop_id`, not displayed from the response, but the misnamed field is a lurking contract violation. | Rename to `shop_id = UUIDField(source='shop_id', read_only=True)` in the serializer. | **DONE 28fe509** |
| 16 | **Med** | A | `views.py:PettyCashAccountView.get:35-40` | §5 / isolation | `PettyCashAccountView.get` calls `PettyCashAccount.objects.get(shop_id=shop_id)` using the URL path parameter directly, with no check that the requesting user has access to that shop. Any authenticated user with `hr.petty_cash.manage` can view any shop's balance by guessing a valid shop UUID. | Verify `shop_id in request.auth.get("shop_ids", []) or request.auth.get("is_tenant_wide")` before the lookup. | **DONE 28fe509** |
| 17 | **Med** | B | `views.py:BudgetAllocationView.post:76-79` / `finance.ts:setBudgetAllocation` | §5 / isolation | `BudgetAllocationView.post` resolves `BudgetHead.objects.get(id=data["head_id"])` with no shop verification. A user with `erp.budget.manage` from Shop A could create a budget allocation against a head belonging to Shop B by submitting the correct head UUID. | Add `BudgetHead.objects.get(id=data["head_id"], shop_id__in=user_shop_ids)`. | **DONE 28fe509** |
| 18 | **Med** | B | `services.py:record_petty_cash_txn:48-51` | §4 | `new_balance = account_locked.current_balance - amount` has no floor check. A debit transaction can drive the petty cash balance negative without any error. Shop operators would see a negative balance card with no explanation. | Add `if data["txn_type"] == TxnType.DEBIT and account_locked.current_balance < amount: raise BusinessRuleViolation("Insufficient petty cash balance.")` before the debit calculation. | *(SKIPPED — spec conflict: test file explicitly documents "overdraft scenario — spec doesn't block it"; floor check contradicts spec)* |
| 19 | **Low** | A | `serializers.py:BudgetHeadSerializer:40-43` / `finance.ts:BudgetHead` | §6 | `BudgetHeadSerializer.fields` includes `"shop"` (FK UUID) not `"shop_id"`. FE `BudgetHead.shop_id` always `undefined`. Low impact today but misleading for any future code that reads `head.shop_id`. | Rename to `shop_id = UUIDField(source='shop_id', read_only=True)`. | **DONE 28fe509** |
| 20 | **Low** | B | `services.py:record_petty_cash_txn:68-73` / `services.py:_update_budget_allocation:138-144` | §8 | Both low-balance petty cash alerts and over-budget alerts are stub log messages only — no WhatsApp notification fires. Spec §8 lists both as required notification triggers. | Implement WhatsApp notifications (same pattern as CRM); dispatch via Celery to avoid blocking the atomic transaction. |

---

## Module 11 — Reports & Dashboard

**Verdict: ~52% aligned (pre-fix). Architecture is sound; the export pipeline and 5 catalogue slugs were broken. Top 3 fixes: (1) fix `requestExport` URL from `/reports/${type}/export/` to `/reports/${type}/?export=format` — all 37 CSV/PDF exports are permanently 404, (2) wire the Celery export task (currently commented out) — even with the correct URL, every export stays queued forever, (3) align the 5 mismatched report slugs so those reports don't always 404.**

**Status: All findings resolved in 5a7fe73 except #15 (job_turnaround_time `closed_at`), which is SKIPPED — requires a `JobTicket.closed_at` schema migration, out of hardening scope.**

| # | Severity | Lens | File : line or MISSING | Spec ref | Finding | Suggested fix |
|---|---|---|---|---|---|---|
| 1 | **Critical** | A | `reports.ts:requestExport:100-108` / `views.py:ReportView.get:308-321` | §6 | `reportApi.requestExport` calls `GET /reports/${type}/export/` — a two-segment path that doesn't match `path("<str:report_type>/", ...)` in `urls.py`. Every export button click returns 404. The actual BE trigger is a query param `?export=csv\|pdf` on the same single-segment URL. | Change `requestExport` to call `` apiGet(`/reports/${type}/`, { ...filters, export: format }) `` (no `/export/` suffix). | **DONE 5a7fe73** |
| 2 | **Critical** | B | `views.py:ReportView.get:317` | §6 / async | `# tasks.run_export.delay(str(job.id))` is commented out. Every export creates an `ExportJob` with status `QUEUED` but the Celery task that processes it is never dispatched. Export jobs are permanently stuck at `queued`; `file_url` is always `""`; FE polls but never sees `ready`. All 37 PDF/CSV exports across the catalogue are permanently broken. | Uncomment `tasks.run_export.delay(str(job.id))` and implement `run_export` in `reports/tasks.py` (query → render → upload → set `file_url` + `status=ready`). | **DONE 5a7fe73 — CSV pipeline implemented; PDF raises NotImplementedError pending weasyprint** |
| 3 | **Critical** | A | `reports.ts:REPORT_CATALOGUE` / `views.py:REPORT_REGISTRY:251-281` | §6 | Five catalogue slugs don't match the BE registry, so `REPORT_REGISTRY.get(report_type)` returns `None` → 404 for each: (a) `outstanding-dues-repair` → registry key is `outstanding-dues`; (b) `pl-summary` → registry key is `pnl-summary`; (c) `gstr-1` → has a dedicated `GSTR1View` at `/reports/gstr1/`, not in registry; (d) `gstr-2` → dedicated `GSTR2View` at `/reports/gstr2-proxy/`, not in registry; (e) `supplier-payable` → registry key is `supplier-payable-aged`. Clicking any of these 5 cards always shows "Failed to load report." | (a)–(b): add aliases in `REPORT_REGISTRY` or rename the FE slug to match. (c)–(d): add separate `reportApi` calls for GSTR endpoints and a dedicated download-only card that bypasses `GenericTable`. (e): add alias `"supplier-payable": ("erp", _supplier_payable)` to the registry. | **DONE 5a7fe73 — all 5 aliases added; gstr-1/gstr-2 wired to JSON wrapper functions over the existing CSV services** |
| 4 | **Critical** | A | `reports.ts:pollExportJob:110-112` / `urls.py` | §6 | `reportApi.pollExportJob` calls `GET /reports/export-jobs/${jobId}/` — a detail endpoint that doesn't exist. Only `export-jobs/` (list) is registered. The 3-second polling loop in `[type]/page.tsx:164-176` always gets 404; jobs never transition from `queued` to `ready` in the UI tray. | Add `path("export-jobs/<uuid:job_id>/", views.ExportJobDetailView.as_view())` returning the single `ExportJob` serialised, and update `pollExportJob` to call that URL. | **DONE 5a7fe73** |
| 5 | **High** | A | `reports.ts:REPORT_CATALOGUE:65` / `views.py:REPORT_REGISTRY:267` | §5 | Permission mismatch for `commission-ledger`: FE catalogue has `permission: 'reports.repair.view'` (so the card is visible to repair managers). BE registry maps the slug to module `"hr"`, constructing gate `"reports.hr.view"`. A user with `reports.repair.view` but not `reports.hr.view` sees the card but gets 403 when they open it. | Align: change FE `REPORT_CATALOGUE` entry for `commission-ledger` to `permission: 'reports.hr.view'`, or change BE registry module to `"repair"`. | **DONE 5a7fe73 — aligned FE to `reports.hr.view`** |
| 6 | **High** | A | `reports.ts:DashboardData` / `services.py:dashboard:152-165` | §6 | BE `dashboard()` returns `revenue_today`, `revenue_month`, `outstanding_amount`, `revenue_trend[*].revenue` as Decimal strings (`"0.00"`), not numbers. FE `DashboardData` types them as `number`. `dashboard/page.tsx:85` checks `data.outstanding_amount > 0` — works by JS coercion. But `dashboard/page.tsx:166` passes `revenue` string to Recharts `<Bar>`, which coerces silently. `money(data.revenue_today)` — if `money()` doesn't handle strings, the KPI cards show `₹NaN`. | Return `float(revenue_today)` etc. from `dashboard()`, or document the string contract and change all FE types to `string`. | **DONE 5a7fe73 — `dashboard()` now returns `float()` for all money fields and trend values** |
| 7 | **High** | A | `services.py:GenericTable / reports.ts:CellValue` / every report service | §6 | Every report service returns Decimal amounts as strings via `_d(...)` (e.g. `"1234.56"`). `CellValue` only applies `money()` formatting when `typeof val === 'number'`. String amounts fall through to the plain `<span>{val}</span>` branch — rendered as `"1234.56"` without `₹` symbol or locale formatting. Every amount column in every report table (revenue, outstanding, commission, salary, etc.) is displayed as a bare number string. | Either (a) return `float(...)` from all service functions so amounts arrive as JS numbers, or (b) add a string branch to `CellValue`: `if (typeof val === 'string' && isMoneyCellKey(colKey)) return <span>{money(parseFloat(val))}</span>`. | **DONE 5a7fe73 — added numeric-string branch to `CellValue` and summary-metric rendering** |
| 8 | **High** | B | `services.py:purchase_summary:788-792` | §6 report data | `purchase_summary` iterates `for po in qs: by_supplier[po.supplier.name] = by_supplier.get(po.supplier.name, _ZERO)` — the current PO value is never added to `by_supplier`. Every supplier shows ₹0.00 in the Purchase Summary report regardless of actual PO value. The correct line is `by_supplier[...] = by_supplier.get(..., _ZERO) + po.grand_total`. | Change to `by_supplier[po.supplier.name] = by_supplier.get(po.supplier.name, _ZERO) + (po.grand_total or _ZERO)`. | **DONE 5a7fe73** |
| 9 | **Med** | B | `services.py:commission_ledger:543-547` / `services.py:technician_performance:511-515` | §6 | Both `commission_ledger` and `technician_performance` filter by `job__created_at__date__gte/lte` — meaning commissions accrued in month M for jobs opened in month M, regardless of when they closed. If a job opens in January and closes in February, its commission appears in January's ledger. This is inconsistent with when commissions are earned (at job closure). | Change filter to `job__closed_at__date__gte=start, job__closed_at__date__lte=end` (matching the Module 08 finding). | **DONE 5a7fe73 — `JobTicket` has no `closed_at` field (confirmed in Module 08 #4); filtered on `TechnicianCommission.created_at` instead, which is the established closure-time proxy** |
| 10 | **Med** | B | `views.py:_shop_ids_from_request:38-44` | §5 | For a `is_tenant_wide` user with no `?shop_id=` param, the function returns `[]`. `_shop_filter([])` returns `Q(pk__in=[])` — matches nothing. All dashboard KPIs and any reports run without a shop filter show 0/empty for org-wide admins. The UX implication is that a shop owner with multi-shop access sees zero open jobs on their dashboard if they're in "all shops" mode. | When `shop_ids == []` and `is_tenant_wide`, skip the shop filter entirely (return an unfiltered queryset) or return all shop IDs for the tenant. Add a visual "no shop selected — showing all shops" indicator in the FE filter bar. | **DONE 5a7fe73 — returns all tenant `Shop` IDs when tenant-wide with no `?shop_id=`; FE indicator left as a follow-up UX item** |
| 11 | **Med** | A | `reports.ts:REPORT_CATALOGUE:52-84` / `views.py:REPORT_REGISTRY` | §6 | Three reports in `REPORT_CATALOGUE` have no matching entry in `REPORT_REGISTRY` beyond the 5 already noted: `inventory-valuation` is in the registry ✓ but `amc-contract-summary` spec filter includes `status` — `_amc_contract_summary` ignores the `status` qp and always returns all statuses. The FE `status` filter dropdown on this report does nothing. | Add `if s := qp.get("status"): qs = qs.filter(status=s)` to `amc_contract_summary`. | **DONE 5a7fe73 — `_amc_contract_summary` now passes `qp.get("status", "")` through to the service** |
| 12 | **Med** | C | `reports/[type]/page.tsx:205` | — | The `queryKey` for report data is `[qk.revenueReport(filters)[0], type, filters]`. `qk.revenueReport` is a revenue-specific query key — using its `[0]` element (the string `"revenue-report"`) as a prefix for all 27 report types means every report shares the same cache namespace prefix. Two different report types with the same filters could theoretically collide. | Use a consistent key factory: `["report", type, filters]`, or add `qk.report: (type, filters) => ["report", type, filters] as const` to `keys.ts`. | **DONE 5a7fe73 — added `qk.report`, replaced the unused `revenueReport`/`hrReport`/`gstReport`/`repairReport`/`inventoryReport` keys (only the hacky `[0]` usage existed)** |
| 13 | **Low** | B | `views.py:ReportView.get:310` | §6 | `ExportJob.filters = dict(request.query_params)` captures a Django `QueryDict`. `dict(QueryDict)` maps each key to a `[list_of_values]` instead of a scalar. When the export Celery task (once implemented) reads `job.filters`, it will receive `{'date_from': ['2024-01-01']}` instead of `{'date_from': '2024-01-01'}` and fail to pass them to service functions expecting scalars. | Use `request.query_params.dict()` (not bare `dict(...)`) which maps each key to its last value. | **DONE 5a7fe73** |
| 14 | **Low** | A | `reports.ts:ExportJob` / `views.py:ExportJobListView.get:332-338` | §6 | `ExportJobListView` uses `.values(...)` that omits `completed_at`. FE `ExportJob.completed_at` is always `undefined`. Low impact since the FE export tray doesn't display completion time — but the `ExportJob` interface is misleading. | Add `"completed_at"` to the `.values(...)` tuple. | **DONE 5a7fe73** |
| 15 | **Low** | B | `services.py:job_turnaround_time:420-421` | §6 | `delta = (job.updated_at.date() - job.created_at.date()).days` uses `updated_at` as the close timestamp. A job whose status changes for any reason (e.g. a notes edit) after closure will have a larger-than-actual `updated_at`. For closed jobs the correct field is `closed_at` (if it exists on the model) or the timestamp of the status-→-closed transition. | Use `job.closed_at.date()` (if `closed_at` exists on `JobTicket`) or filter stages for the final closing stage timestamp. | *(SKIPPED — `JobTicket` has no `closed_at` field and no reliable closure-timestamp proxy exists on the model (only generic `created_at`/`updated_at` from `BaseModel`); fixing this correctly requires a schema migration adding `closed_at` and populating it in `repair/services.py` on the close transition — a model change beyond hardening scope, flagging for spec/schema follow-up)* |

---

## Module 12 — Authentication & Settings

**Verdict: Auth core is solid; Settings backend is entirely absent — 18 endpoints are missing, making every settings page permanently broken (all 404).**
**Status: Findings #2–9 resolved in 1f9da5b. #1 (Settings backend) is SKIPPED — the audit itself frames it as "a full module build, not a patch" (Pattern 6); building 18 endpoints + serializers is new-feature work, out of hardening scope.**

| # | Severity | Lens | File : line or MISSING | Spec ref | Finding | Suggested fix |
|---|---|---|---|---|---|---|
| 1 | **Critical** | B | `config/urls.py` / MISSING: `/api/v1/users/*`, `/api/v1/roles/*`, `/api/v1/permissions/`, `/api/v1/whatsapp/*`, `/api/v1/notifications/templates/*`, `/api/v1/shops/<id>/`, `/api/v1/tenants/me/` | §2 RBAC / §1 settings | The Settings backend module does not exist. `config/urls.py` registers no routes for users, roles, permissions, WhatsApp, notification templates, shop detail/update, or tenant branding. All 18 endpoints called by `settingsApi` return 404: `GET/POST /users/`, `PATCH/POST /users/<id>/`, `POST /users/<id>/force-logout/`, `GET/POST/PATCH/DELETE /roles/*`, `GET /permissions/`, `GET/POST/POST /whatsapp/*`, `GET/PATCH /notifications/templates/*`, `GET/PATCH /shops/<id>/`, `GET/PATCH /tenants/me/`. Every settings page — Users, Roles, Shop Profile, WhatsApp & Notifications — is permanently broken on first load. | Implement and register a `settings` app (or split across existing apps) with views + serializers for all 18 endpoints and include them in `config/urls.py`. **SKIPPED (1f9da5b)** — the audit's own Pattern 6 calls this "a full module build, not a patch": 18 endpoints + serializers + permissions is new-feature scope, conflicting with "hardening only." Left flagged for a dedicated feature pass. |
| 2 | **Critical** | A | `authentication/views.py:OTPVerifyView:237-242` | §2 auth | `OTPVerifyView` increments `cached["attempts"]` on wrong OTP but never enforces a maximum — there is no `if cached["attempts"] >= MAX: raise ...` guard. Within the 600-second TTL window an attacker can brute-force all 10^6 combinations (avg 500 k requests) with no lockout. The `attempts` field is tracked but never read. | Add `MAX_OTP_ATTEMPTS = 5` (or read from settings). After incrementing, if `cached["attempts"] >= MAX_OTP_ATTEMPTS`: delete the cache key and raise `ValidationError({"otp": ["Too many attempts. Request a new OTP."]})`. **DONE (1f9da5b)** — added `settings.MAX_OTP_ATTEMPTS = 5`; verify now deletes the cache key and raises a lockout error once the threshold is hit. New test `test_otp_verify_max_attempts_locks_out` covers it. |
| 3 | **High** | B | `authentication/views.py:OTPRequestView:212-215` / `_send_otp` | §2 auth | `_send_otp` is a debug-log-only stub in production — `msg91_service.send_otp(phone, otp)` is commented out. Phone-based login silently "succeeds" (returns `{"message": "OTP sent."}`) but no SMS is ever dispatched. Users who try to log in via OTP will never receive a code and cannot proceed. | Implement the MSG91 (or equivalent) integration and remove the stub comment. Until integrated, return an explicit 503 with `{"code": "SMS_NOT_CONFIGURED"}` rather than a false-success 200. **DONE (1f9da5b)** — `_send_otp` now returns a bool; `OTPRequestView` returns `503 SMS_NOT_CONFIGURED` (no cache write, no false "sent" message) when dispatch fails outside DEBUG. Full MSG91 integration remains a follow-up. Existing OTP-flow tests now opt into dev-mode dispatch via an `override_settings(DEBUG=True)` fixture (Django forces `DEBUG=False` for test runs); a new `TestOTPSendUnconfigured` covers the production-mode 503 path. |
| 4 | **High** | A | `settings/users/page.tsx:77` | §2 RBAC | `inviteMutation` calls `settingsApi.inviteUser({ ...v, role_ids: [] })` — `role_ids` is hardcoded to an empty array. The invite form has no role-selection UI. Even when the backend is implemented, invited users will always be created with no roles and no permissions, requiring a separate update step that is not wired up. | Add a role multi-select field to the invite form. The form already fetches `rolesData?.items` — pass the selected role IDs from a `useState` into the mutation body. **DONE (1f9da5b)** — added a checkbox-list role multi-select to the invite dialog backed by `useState<string[]>`, wired into `inviteMutation`'s `role_ids`. |
| 5 | **Med** | B | `authentication/views.py:DevOTPView:40-55` | §2 auth | `DevOTPView` is only guarded by `if not settings.DEBUG: raise PermissionDenied`. If a staging environment has `DEBUG=True` (common during rollout), the endpoint is fully public and exposes any cached OTP for any phone number. There is no IP allow-list, `INTERNAL_IPS` check, or additional env flag guard. | Add a second guard: `if not settings.DEBUG or not getattr(settings, 'DEV_OTP_ENABLED', False): raise PermissionDenied`. Require an explicit `DEV_OTP_ENABLED=True` in `.env` so staging default is safe. **DONE (1f9da5b)** — added `settings.DEV_OTP_ENABLED` (env-driven, defaults `False`) and the second guard exactly as suggested. |
| 6 | **Med** | A | `settings/whatsapp/page.tsx:75,80` | — | The WhatsApp and notification templates page uses bare string literals as query keys: `queryKey: ['whatsapp-connection']` and `queryKey: ['notif-templates']` instead of the `qk.*` factory used everywhere else. If either key is referenced in another component without matching the exact string, React Query won't share cache or invalidate consistently. | Add `whatsAppConnection: () => ['whatsapp-connection'] as const` and `notifTemplates: () => ['notif-templates'] as const` to `keys.ts`, and import them in the WhatsApp page. **DONE (1f9da5b)** — `notifTemplates` already existed in `keys.ts`; added `whatsAppConnection`, and switched the WhatsApp page's queries/mutations/cache writes to `qk.*`. |
| 7 | **Med** | A | `authentication/views.py:LoginView:148-161` | §2 auth | `LoginView` reads `tenant_slug` from JWT context but falls back to `request.data.get("tenant_slug", "")` if context is empty. A request body can supply an arbitrary `tenant_slug`, potentially checking the suspended status of a different tenant than the one actually being accessed. In a multi-tenant proxy setup this could be used to test suspension status of any tenant. | Remove the `request.data.get("tenant_slug")` fallback; derive tenant identity exclusively from the request's subdomain/JWT context. If the slug is missing, reject with 400 rather than continuing without a tenant check. **DONE (1f9da5b)** — removed the body-param fallback; tenant identity now comes solely from `_get_tenant_slug` (resolved subdomain/JWT context). Did **not** apply the suggested "reject with 400 if slug is missing": that would 400 every legitimate login that has no resolvable tenant context (local dev, platform-admin, the autouse test fixture that pins the alias to `"default"`) — the spec's normal-operation needs win over that part of the suggested fix. Updated `test_suspended_tenant_login_blocked` to simulate a resolved-but-suspended tenant context via `set_tenant_db_alias` (the legitimate path; the body-param probe it previously relied on is exactly what was removed). |
| 8 | **Low** | B | `authentication/serializers.py:PasswordChangeSerializer:102-105` | §2 auth | The password complexity error message (`"Password must be at least 8 characters and include at least one uppercase letter, one number, and one special character."`) is returned verbatim in the 400 response. This discloses the full policy to unauthenticated-adjacent callers, aiding targeted brute-force construction. | Return a generic message (`"Password does not meet complexity requirements."`) and document the rules only in the app's help text / onboarding UI. **DONE (1f9da5b)** — message replaced verbatim as suggested; no test asserted on the old text. |
| 9 | **Low** | B | `master/views.py:TenantListView:83-85` | §1 platform admin | `TenantListView` (platform admin only) returns a flat array — no `{items, meta}` pagination. For a SaaS with many tenants this endpoint will grow unbounded. The FE platform-admin tenant list (if one exists) would need updating when pagination is added. | Wrap in `{items, meta}` and add cursor-based pagination consistent with other list views before tenant count grows large. **DONE (1f9da5b)** — wrapped in `RepairOSCursorPagination` / `{items, meta}`. Confirmed this was a live Lens-A break, not just future-proofing: `platformApi.listTenants` and `/platform/tenants` already destructure `data.items`/`data.meta` — the FE was already coded against the paginated shape the BE wasn't returning. |

---

## Cross-Module Summary — Systemic Issues & Fix Priority

> 12 modules audited. 9 systemic patterns account for the majority of broken flows. Fix in priority order below — each pattern, when fixed once per affected module, unblocks the most user-facing surface area per unit of work.

### Pattern 1 — WhatsApp stubs: zero of 31 templates ever send (ALL modules)

Every `_send_whatsapp` / `_broadcast` / `_send_otp` / `_send_otp` function across every module is a debug-log stub. Confirmed in: CRM (`crm/tasks.py`), Repair (`repair/services.py`), POS (`pos/services.py`), AMC (`amc/tasks.py`), Inventory (`inventory/services.py`), HR (`hr/services.py`), Finance (`finance/services.py`), Auth (`auth/views.py`). The product markets 31 WhatsApp notification templates. None fire. **Fix**: implement a shared `notifications.send_whatsapp(phone, template_name, variables)` service backed by Meta Cloud API and swap all stubs to call it.

### Pattern 2 — Celery beat never scheduled (Modules 01, 02, 03, 04, 11)

`CELERY_BEAT_SCHEDULE` in `config/settings/base.py` is empty. Affected tasks that silently never run: CRM `mark_overdue_tasks` + `send_task_daily_digest` (01), Repair `send_warranty_expiry_reminders` (02), POS `send_wholesale_payment_reminders` (03), AMC `mark_missed_visits` + `send_renewal_reminders` + `send_visit_reminders` + `process_auto_renewals` (04), Reports `run_export` (11). **Fix**: populate `CELERY_BEAT_SCHEDULE` with all defined tasks in one PR; the tasks already exist — they just need schedule entries.

### Pattern 3 — Pagination shape: flat `[]` vs expected `{items, meta}` (Modules 06, 09, 10, 11, 12)

Every list view in HR (09) and Finance (10) returns a bare array. FE universally reads `data?.items ?? []` → always empty. Affected: HR employees, attendance, leave requests, salary slips; Finance petty cash transactions, expenses, budget heads, assets, budget allocations; Procurement suppliers (06); Reports export-job list (11); Settings roles/permissions (12). **Fix**: apply `CursorPagination` + `get_paginated_response({items, meta})` wrapper to all list views — one shared mixin, applied to each missing view.

### Pattern 4 — Shop isolation absent on detail/mutation views (Modules 01, 03, 04, 09, 10)

List views filter by `shop_ids__in`; `get_object()` resolves from the unfiltered queryset. Any user with a valid permission can read or modify any record from any shop or tenant by guessing a UUID. Confirmed: AMC `AMCVisitViewSet` (04), POS `SalesReturnViewSet` (03), HR detail views (09), Finance expense/asset detail views (10). **Fix**: add `.filter(shop__in=shop_ids)` (or equivalent via `select_related`) inside `get_queryset` for all `APIView`-based detail views, not just list views.

### Pattern 5 — FK field naming: DRF emits `shop` / `employee` / `assigned_technician`, FE expects `_id` suffix (Modules 01, 02, 04, 09, 10)

DRF default `PrimaryKeyRelatedField` serializes as the bare field name; every module's FE interface appends `_id`. Silent `undefined` results on: `shop_id` (HR, Finance), `employee_id` + `employee_name` (HR leave/slip), `assigned_technician_id` + `_name` (AMC, Repair), `customer_id` (CRM, POS), `head_id` (Finance budget). **Fix**: systematic pass over all serializers — add explicit `UUIDField(source='x_id', read_only=True)` aliases and `CharField(source='x.name', read_only=True)` SerializerMethodFields wherever FE expects them.

### Pattern 6 — Settings backend entirely missing (Module 12)

18 endpoints called by `settingsApi` return 404 (`/users/`, `/roles/`, `/permissions/`, `/whatsapp/*`, `/notifications/templates/*`, `/shops/<id>/`, `/tenants/me/`). No settings page works. **Fix**: implement a `settings` Django app with views, serializers, and URL registration — this is a full module build, not a patch.

### Pattern 7 — Stock deduction stubs: inventory never decremented (Modules 02, 03)

`_deduct_stock` in POS (03) and `repair_out` movements in Repair (02 `_on_close`) are debug-log stubs or simply absent. Every completed sale and every closed repair job with received spare parts leaves inventory unchanged. **Fix**: implement `StockMovement` writes (`sale_out`, `repair_out`, `return_in`) inside the respective service atomic transactions.

### Pattern 8 — Money type mismatch: Decimal strings returned, FE types as `number` (Modules 10, 11)

All Finance and Reports services return Decimal amounts as strings via `_d()` (e.g. `"1234.56"`). FE types them as `number`. `money()` formatting skips strings → amounts render without ₹ symbol. Dashboard KPI cards may show `₹NaN` if `money()` doesn't coerce. **Fix**: either return `float(val)` from all service functions, or change all FE money types to `string` and update `money(parseFloat(val))` at render points.

### Pattern 9 — Reports export pipeline broken end-to-end (Module 11)

Three compounding issues make all 37 PDF/CSV exports permanently fail: (a) `requestExport` calls the wrong URL (path suffix instead of query param); (b) the Celery export task is commented out so every job stays `QUEUED` forever; (c) `pollExportJob` calls a detail endpoint that doesn't exist (404). **Fix**: fix the URL, uncomment + implement `run_export`, add the export-job detail route — all three are required together for any export to work.

### Recommended fix order (maximum user-visible impact per sprint)

| Priority | Pattern | Modules | Impact |
|---|---|---|---|
| 1 | Pagination wrapper (`{items, meta}`) | 06, 09, 10, 11, 12 | Unbreaks all HR, Finance, Procurement, Reports list tables |
| 2 | FK field `_id` / `_name` aliases in serializers | 01–05, 09, 10 | Unbreaks technician/employee/customer display across 7 modules |
| 3 | Shop isolation on detail views | 01, 03, 04, 09, 10 | Security: closes cross-tenant UUID enumeration |
| 4 | Settings backend (new app) | 12 | Unbreaks all user/role/shop/WA settings — required before first customer |
| 5 | Celery beat schedule | 01, 02, 03, 04, 11 | Enables all scheduled work: overdue tasks, reminders, auto-renewals, exports |
| 6 | Reports export pipeline (3-part fix) | 11 | Unbreaks all 37 PDF/CSV exports |
| 7 | WhatsApp shared service | All | Enables all 31 notification templates |
| 8 | Stock deduction | 02, 03 | Inventory correctness: sales/repairs stop silently ignoring stock |
| 9 | Money type alignment | 10, 11 | ₹ formatting in Reports and Finance |
| 10 | OTP brute-force guard | 12 | Auth security: max-attempts enforcement on OTP verify |
